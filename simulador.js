// ================================================================
// SIMULADOR.JS - Destiny Draw Yugioh
// Módulo: Pestaña SIMULADOR
// ================================================================

console.log('simulador.js loaded');

function renderSimuladorSection() {
    const section = document.getElementById('simulator');
    section.innerHTML = `
        <h2 class="text-2xl mb-4">Simulador - EN DESARROLLO</h2>
        <p>Este módulo está en desarrollo. Funcionalidad completa próximamente.</p>
    `;
}

document.addEventListener('DOMContentLoaded', () => {
    renderSimuladorSection();
});

console.log('simulador.js loaded');
