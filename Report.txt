# DESTINY DRAW — REPORTE TÉCNICO
**Versión:** v2.4 | **Fecha:** Mar 2026
**Stack:** Vanilla JS (ES6 objects) · CSS modular · HTML5 SPA · YGOProDeck API · localStorage

---

## 1. ESTRUCTURA DE ARCHIVOS ACTUAL

### 1.1 HTML
```
index.html  — Shell única. Secciones vacías, JS llena el DOM en runtime.
```

### 1.2 JavaScript — 11 módulos + 1 módulo de datos por defecto

**Orden de carga en index.html (crítico):**
```
core.js → data.js → buscador.js →
mideck.js → historial.js → estadisticas.js →
simuladores.js → simuladores-campo.js → formacion.js →
default_data.js → logger.js  (SIEMPRE AL FINAL)
```

| Archivo | Objetos globales | Líneas aprox. | Descripción |
|---------|-----------------|--------|-------------|
| `logger.js` | `DDLogger` | 365 | Logger no-invasivo. Envuelve todos los objetos globales post-`window.load`. API: `DDLogger.exportReport()`, `getLogs()`, `getStats()`, `enable/disable/clear()`. Intercepta localStorage y errores globales. |
| `core.js` | `Navigation`, `Shortcuts`, `ContentManager`, `switchTab` | 487 | navigation + utils + contentmanager |
| `data.js` | `ConfigManager`, `Stats`, `SpecialtyAnalyzer`, `NomenclatureAnalyzer` | ~2226 | Config global (incl. Scoring G1/G2), cálculo de scores, analizadores de carta. Incluye `duplicateNomenclatureCategory`. |
| `buscador.js` | `Buscador`, `CardViewer`, `Favoritas` | 1932 | buscador + cardviewer + favoritas + botón Carta Random |
| `mideck.js` | `Deck`, `Banlist`, `Engines` | ~3020 | deck + banlist + engines |
| `historial.js` | `Matchups`, `Duelista` | 783 | matchups + duelista |
| `estadisticas.js` | `Estadisticas` | ~2525 | scores, meta, cross-score, análisis de deck. `recalculateAllMetaDeckScores` es ahora async con enriquecimiento automático. |
| `simuladores.js` | `Winrate`, `Torneo`, `DueloEnVivo`, `Hipergeometria` | 2819 | winrate + torneo + duelo en vivo + hipergeometría |
| `simuladores-campo.js` | `ZonaPractica`, `Experimentacion` | 3228 | zonapractica + experimentacion |
| `formacion.js` | `Formacion`, `Meta`, `Config`, `Welcome`, `MusicPlayer`, `HelpPanel` | ~3661 | formacion + meta + config completo (Scoring G1/G2, Nomenclatura con pencil/clone/delete, RPS Ninguno) + welcome + help |
| `default_data.js` | `DefaultData` | 1194 | Inyección única de datos por defecto. Parchea `Config.resetToDefault`. |

**Nota:** `Stats` vive dentro de `data.js`. `Winrate` y `DueloEnVivo` son los objetos correctos en `simuladores.js`; el antiguo `Duelo` ya no existe.

### 1.3 CSS — 6 archivos (consolidados)

**Orden de carga en index.html:**
```html
style_core.css → style_buscador.css → style_mideck.css →
style_simuladores.css → style_config.css → responsive_style.css
```

| Archivo | Líneas aprox. | Absorbe |
|---------|--------|---------|
| `style_core.css` | 1322 | style_global + style_welcome + style_cardviewer |
| `style_buscador.css` | 1016 | — |
| `style_mideck.css` | 2194 | — |
| `style_simuladores.css` | 5842 | style_simuladores + style_estadisticas |
| `style_config.css` | ~2260 | style_config + style_formacion + style_meta + clases de Scoring G1/G2 |
| `responsive_style.css` | 558 | Todos los @media del proyecto |

**Todos los archivos CSS tienen sus `@media` eliminados** — viven exclusivamente en `responsive_style.css`.

---

## 2. ESTRUCTURA DEL DOM

| ID sección | Pestaña | Llenado por |
|-----------|---------|-------------|
| `#buscador-content` | Buscador | HTML estático + `Buscador.init()` |
| `#mideck-content` | Mi Deck | `Deck.render()` → `#deck-container` |
| `#estadisticas-content` | Estadísticas | `Estadisticas.render()` |
| `#simuladores-content` | Simuladores | `Torneo.init()` (lazy) |
| `#meta-content` | Meta | `Meta.init()` — fuera de `Navigation.tabs` |
| `#formacion-content` | Formación | `Formacion.init()` |
| `#config-content` | Config | `Config.render()` |

**Navigation.tabs:**
```javascript
['buscador', 'mideck', 'estadisticas', 'simuladores', 'formacion', 'config']
```
`meta` está oculto en el nav HTML y no figura en `Navigation.tabs`; se activa internamente vía `Meta.init()` o `switchTab('meta')`.

---

## 3. LOCALSTORAGE — TODAS LAS CLAVES

| Clave | Módulo | Tipo | Descripción |
|-------|--------|------|-------------|
| `yugioh_config` | `ConfigManager` | Object | Config principal: roles, roleConditions, roleWeights, staples, specialties, nomenclature, pillars, pillarRPS, **g1g2Roles, roleBasePower, reliabilityTable, scoringLayers**, metaMasters, metaLinks, formacionGames, formacionTopics, shortcuts, music |
| `yugioh_player_level` | `ConfigManager` | String | `'novato'\|'casual'\|'competitivo'\|'default'` |
| `dd_player_profile` | `ContentManager` | String | Controla visibilidad de secciones |
| `dd_content_visibility` | `ContentManager` | Object | Override manual de visibilidad por sección |
| `dd_welcome_dismissed` | `Welcome` | String `'true'` | Panel reaparece solo al Borrar Data |
| `yugioh_favoritas` | `Favoritas` | Object | `{cardId: {id,name,type,img,data}}` |
| `yugioh_engines` | `Engines` | Array | `[{id,name,cards,roles,notes,cover}]` |
| `yugioh_decks` | `Deck` | Object | Decks guardados `{nombre: {cards,notes}}` |
| `matchup_${deckName}` | `Matchups` | Array | `[{opponent,wins1st,losses1st,wins2nd,losses2nd,notes}]` |
| `yugioh_music_config` | `ConfigManager` | Object | `{enabled, volume, tracks:{novato,casual,competitivo}}` |
| `yugioh_meta_fallbacks` | `ConfigManager` | Object | Imágenes fallback para Maestros |
| `yugioh_formacion_fallbacks` | `ConfigManager` | Object | Imágenes fallback para Juegos |
| `yugioh_formacion_notes` | `Formacion` | Array | `[{title,content,date}]` |
| `yugioh_formacion_mastered` | `Formacion` | Array | IDs de temas dominados |
| `yugioh_power_cache` | `Estadisticas` | Object | Cache de Power Scores calculados |
| `yugioh_cross_scores` | `Estadisticas` | Object | Cross-scores entre decks del meta (base del External Score del meta) |
| `yugioh_meta_decks` | `Estadisticas` | Array | Decks del meta importados (.ydk) |
| `yugioh_meta_deck_scores` | `Estadisticas` | Object | Scores calculados de cada deck del meta |
| `yugioh_meta_card_library` | `Estadisticas` | Object | Librería de cartas del meta (cache local de la API) |
| `dd_report_counter` | `DDLogger` | Number | Contador de reportes exportados |

**Flags de DefaultData** (sobreviven a Borrar Data, se eliminan solo con Restaurar Config):

| Clave | Descripción |
|-------|-------------|
| `dd_default_decks_loaded` | Decks de ejemplo ya inyectados |
| `dd_default_notes_loaded` | Apuntes por defecto ya inyectados |
| `dd_default_staples_loaded` | Staples por defecto ya inyectados |
| `dd_default_specialties_loaded` | Especialidades por defecto ya inyectadas |
| `dd_default_engines_loaded` | Engines por defecto ya inyectados |
| `dd_default_games_loaded` | Juegos de Formación por defecto ya inyectados |
| `dd_default_masters_loaded` | Maestros del Meta por defecto ya inyectados |
| `dd_default_genesys_loaded` | Banlist Genesys ya generado desde API |

**Nota:** `g1g2Roles`, `roleBasePower`, `reliabilityTable` y `scoringLayers` viven dentro de `yugioh_config`. Export, Import, Borrar (Config), Reporte y Restaurar Config los cubren automáticamente.

**Zona de Borrado selectivo — opciones disponibles:**

| data-key | Claves eliminadas | En memoria |
|----------|------------------|------------|
| `decks` | `yugioh_decks`, prefijos `deck_` | `Deck.cards = {}` |
| `engines` | `yugioh_engines` | — |
| `matchups` | prefijos `matchup_` | — |
| `winrates` | `yugioh_winrates`, `pz_winrate_standalone` | — |
| `favoritas` | `yugioh_favoritas` | — |
| `torneo` | `yugioh_torneo_actual` | — |
| `practica` | prefijos `pz_states_` | — |
| `cache` | `yugioh_power_cache`, `yugioh_cross_scores`, `yugioh_meta_deck_scores` | `powerScoreCache=null`, `crossScores={}`, `metaDeckScores={}` |
| `meta_library` | `yugioh_meta_card_library` | `metaCardLibrary={}` |
| `meta_folders` | `yugioh_meta_folders`, `yugioh_meta_decks`, `yugioh_meta_deck_scores`, `yugioh_cross_scores` | `metaDecks={}`, `metaFolders=[]`, `metaDeckScores={}`, `crossScores={}` |
| `formacion` | `yugioh_formacion_notes`, `yugioh_formacion_mastered` | — |
| `banlist` | `yugioh_banlist_data` | — |
| `config` | `yugioh_config` | — |
| `perfil` | `yugioh_player_level`, `dd_player_profile`, `dd_content_visibility`, `dd_welcome_dismissed` | — |
| `fallbacks` | `yugioh_meta_fallbacks`, `yugioh_formacion_fallbacks` | — |

---

## 4. DEPENDENCIAS ENTRE MÓDULOS

```
data.js (ConfigManager)  <── buscador, mideck, historial, estadisticas,
                               simuladores, formacion, core (ContentManager)

data.js (Stats)          <── estadisticas (Internal Score, External Score,
                               G1/G2 Score, Counter Deck Score)

core.js (ContentManager) <── formacion (applyProfile al cambiar perfil)
                               navigation applyAll() en cada tab change

mideck.js (Engines)      <── Favoritas.render() delega si _activeTab='favoritas'
                               Staples: usa campo imageUrl (NO img)

historial.js (Duelista)  <── lee matchup_${deckName} de todos los decks
                               requiere Deck.getSavedDecks()

formacion.js (Meta)      <── Sub-tabs Fuentes y Maestros de Formacion delegan a
                               Meta._renderFuentesSection() / _renderMaestrosSection()

formacion.js (Welcome)   <── llama ConfigManager.savePlayerLevel() Y
                               ContentManager.applyProfile() al hacer enter()

default_data.js          <── requiere ConfigManager, Deck, Engines, Formacion,
(DefaultData)                 Meta, Banlist (todos ya cargados antes en index.html)
                               Parchea Config.resetToDefault al final del load.
```

---

## 5. FLUJOS CRÍTICOS

### 5.1 Cambio de pestaña
```
switchTab(name) → Navigation.showTab(name)
  → oculta todos .tab-content, activa #name-content
  → si mideck:            Engines.init()
  → si simuladores:       Torneo.init()
  → si formacion:         Formacion.init()
  → si meta:              Meta.init()
  → si saliendo de simuladores: ZonaPractica._cleanupFloatBtns()
  → Estadisticas.updateFloatingWidgetVisibility(tabName)
  → ContentManager.applyAll()
  → window.scrollTo({ top:0, behavior:'smooth' })
```

### 5.2 Agregar carta al deck (desde Buscador)
```
Buscador.addCard(index)
  → Deck.syncFromViewer(card.id, card, currentQty + 1)
  → si carta nueva: autoAssignRoles() + SpecialtyAnalyzer + NomenclatureAnalyzer
  → Deck.render()  → recalcula Internal Score + G1/G2 Score en tiempo real
  → External Score se recalcula si powerScoreCache existe
```

### 5.3 Importar .ydk a Mi Deck
```
Deck.importYDK()
  → input[file] .ydk → parseYDK(content, filename)
  → overlay de carga "Importando deck... N cartas"
  → fetch API YGOProDeck (batch por IDs únicos)
  → construye newCards con autoAssignRoles()
  → Deck.cards = newCards; Deck.render(); Deck.onDeckLoaded()
  → elimina overlay → alert "Deck importado"
```

### 5.4 Cálculo de scores — ciclo completo
```
Stats.calculateInternalScore(Deck.cards)
  → loop de roles → rendimientos decrecientes
  → G1/G2 Score paralelo: BasePower(L3) × L1 × L2 × L4 × L5 × Reliability
  → devuelve { internalScore, consistency, power, resilience,
               g1Score, g2Score, penalty, totalCards, mainCards }

Stats.calculateExternalScore(Deck.cards, powerScoreCache, metaDecks)
  → cruza specialties del deck con powerScoreCache
  → RPS modifier (ignorando loser='ninguno')
  → devuelve { externalScore, g1Vulnerability, g2Vulnerability, ... }
```

### 5.5 Actualizar Scores del Meta — flujo automático completo (async)
```
Estadisticas.recalculateAllMetaDeckScores()  ← async
  → lista todos los decks con secciones (sin importar si fueron abiertos antes)
  → crea overlay #meta-update-overlay con spinner + barra de progreso + nombre del deck actual

  FASE 1 (0–60%) Enriquecimiento automático:
    por cada deck en la lista:
      → IDs no presentes en metaCardLibrary → fetch API en batches de 10
      → autoAssignRoles() + SpecialtyAnalyzer para cada carta nueva
    → _saveMetaCardLibrary()

  FASE 2 (60–92%) Cálculo de scores:
    por cada deck:
      → _computeAndSaveMetaDeckScore(folder, filename)
      → Internal Score + pilares con fakeCards desde la librería

  FASE 3 (92–100%) Cross-Score N×N:
    → calculateCrossExternalScores()
    → metaDeckScores[key].externalScore = crossExternalScore
    → guarda yugioh_cross_scores + yugioh_meta_deck_scores

  → "¡Listo! N decks actualizados" 700ms → overlay desaparece → render()
```
Ya no es necesario abrir cada deck manualmente. Un solo clic procesa todos.

### 5.6 Selección de perfil
```
Welcome.enter(tabName, levelKey)
  → ContentManager.applyProfile(levelKey)  → 'dd_player_profile'
  → ConfigManager.savePlayerLevel(levelKey) → 'yugioh_player_level'
  → MusicPlayer.setTrack(cfg.tracks[levelKey])
  → Welcome.dismiss() → 'dd_welcome_dismissed' = 'true'
  → Navigation.showTab(tabName)
```

### 5.7 Borrar Todo / Restaurar Config
```
Config.borrarTodo()
  → localStorage.clear() + _resetModulesInMemory() + Welcome.init()

Config.resetToDefault()  ← parcheado por DefaultData
  → localStorage.clear() + await DefaultData.init()
```

### 5.8 DefaultData.init() — inyección única por tipo
```
window.load → DefaultData.init()
  → _initNotes / _initSpecialties / _initGames / _initMasters
  → _initDecks / _initStaples / _initEngines / _initGenesys  (fetch API)
  → _patchConfig() → sobreescribe Config.resetToDefault
```

---

## 6. SUB-TABS Y SECCIONES

### Mi Deck
```
Sub-tabs: Decklist | Construcción
Sidebar:  Engines | Decks | Staples | Favoritas
```

### Config — secciones colapsables
```
Mecánicas y Roles
  Panel colapsable por rol.
  Header: badge nombre (read-only) | ✏️ focusRenameInput | ⧉ duplicateRole | 🗑️ deleteRole
  Cuerpo: keywords · conditionals · notContains · nomenclaturas · peso en pilares

Counters          — pares mecánica/counter con 🗑️ por par

Staples           — cartas esenciales del formato

Nomenclatura de Efectos
  Panel colapsable por categoría.
  Header: badge coloreado (read-only) | color picker | ✏️ focusNomRenameInput | ⧉ duplicateNomCategory | 🗑️ deleteNomCategory
  Cuerpo: startsWith · contains · notContains · endsWith

Pilares del Internal Score
  Roles por pilar + RPS con opción "Ninguno" (ese pilar no activa modifier)

Scoring Avanzado (G1/G2)
  G1/G2 por rol | Poder Base (L3) | Fiabilidad por copias | Capas L1/L2/L4/L5 en columnas

Rendimientos Decrecientes
Atajos Rápidos
Banlist del Formato
Música
Exportar / Importar Data   (importar pide confirmación; exportar muestra detalle del contenido)
Restaurar Configuración
Zona de Borrado            (15 categorías selectivas, cache y meta_library separados)
```

---

## 7. SISTEMA DE SCORING — DETALLE

### 7.1 Internal Score
`Stats.calculateInternalScore(cards)`. Loop de roles con rendimientos decrecientes.
Devuelve `{internalScore, consistency, power, resilience, penalty, totalCards, mainCards}`.

### 7.2 G1/G2 Score (loop paralelo al Internal Score)
```
CardScore = BasePower(L3) × L1_mult × L2_mult × L4_mult × L5_mult × Reliability

L1 — Velocidad:   keywords en 'condicionActivacion'
L2 — Coste:       keywords en 'costoActivacion'
L4 — Restricción: keywords en 'restriccion'
L5 — Alcance:     keywords en 'efectoGenerico'
Reliability: tabla por copias (1→0.40, 2→0.65, 3→0.85, 4→0.90, 5→0.95)
             + pool indirecto de searchers del mismo arquetipo

g1g2Roles[dominantRole] === 'g1' → g1Score
g1g2Roles[dominantRole] === 'g2' → g2Score
'neutral'                        → 50/50
```

### 7.3 External Score (deck activo)
`Stats.calculateExternalScore()` — deck vs powerScoreCache.
RPS modifier ignorando loser='ninguno'.
Devuelve `{externalScore, g1Vulnerability, g2Vulnerability, ...}`.

### 7.4 External Score (meta) = Cross-Score N×N
`calculateCrossExternalScores()`. Para cada deck A: amenazas de counters de B ponderadas por internalScore de B y RPS. `crossExternalScore = (1 - threat/baseline) × 10`.

### 7.5 RPS
Reglas `[ganador, perdedor]`. `perdedor='ninguno'` → ese pilar no tiene counter. Modificadores: ×1.25 ventaja / ×0.75 desventaja / ×1.0 neutral.

### 7.6 Scoring Avanzado — métodos save en Config

| Función | Qué guarda |
|---------|-----------|
| `saveG1G2Role(rol, valor)` | Clasificación G1/G2/neutral del rol |
| `saveRoleBasePower(rol, val)` | Poder Base L3 (0–10) |
| `saveReliabilityEntry(copias, val)` | Probabilidad por n copias (0–1) |
| `saveLayerEntryKeywords(lKey, idx, raw)` | Keywords de una entrada de capa |
| `saveLayerEntryMultiplier(lKey, idx, val)` | Multiplicador de una entrada |
| `addLayerEntry(lKey)` | Añade entrada vacía a la capa |
| `removeLayerEntry(lKey, idx)` | Elimina entrada por índice |

---

## 8. SISTEMA DE ROLES Y NOMENCLATURA — GESTIÓN EN CONFIG

### 8.1 Detección de roles
```
autoAssignRoles(card) → detectPossibleRoles(card)
  por cada rol en roleConditions:
    keywords coinciden en desc
    AND conditionals (si hay) también coinciden
    AND notContains (si hay) NO coinciden
    → rol asignado

roleConditions[roleName]: { keywords:[], conditionals:[], notContains:[] }
```

### 8.2 Pattern de renombrado con input oculto (roles y nomenclatura)
El badge de nombre es siempre el elemento visible. El input se oculta con `display:none`.

| Elemento | Roles | Nomenclatura |
|---------|-------|-------------|
| Span visible | `id="rpn-${roleName}"` | `id="npn-${cat.id}"` |
| Input oculto | `id="rni-${roleName}"` | `id="nni-${cat.id}"` |
| Botón lápiz | `focusRenameInput(roleName)` | `focusNomRenameInput(catId)` |
| onblur | `renameRole(el)` | `renameNomCategory(catId, el)` |
| Clonar | `duplicateRole(roleName)` → `ConfigManager.duplicateRole` | `duplicateNomCategory(catId)` → `ConfigManager.duplicateNomenclatureCategory` |
| Borrar | `deleteRole(roleName)` | `deleteNomCategory(catId)` |

`duplicateNomenclatureCategory(catId)` — deep-copy de la categoría, nuevo ID `custom_` + timestamp, nombre + " (copia)", insertada inmediatamente después de la original. Devuelve el nuevo ID para scroll.

---

## 9. BUGS RESUELTOS

| # | Bug | Archivo | Solución |
|---|-----|---------|----------|
| 1 | `_calcWR` duplicado 3 veces | `historial.js` | Eliminadas copias extra |
| 2 | `yugioh_player_level` y `dd_player_profile` desincronizados | `formacion.js` | `enter()` escribe ambas claves |
| 3 | Welcome reaparecía al recargar | `formacion.js` | `init()` lee `dd_welcome_dismissed` |
| 4 | `logger.js` referenciado pero inexistente | `index.html` | Nuevo DDLogger creado |
| 5 | MusicPlayer ignoraba perfil guardado | `formacion.js` | Lee nivel al inicio |
| 6 | Botón ▶ pausaba en lugar de detener | `formacion.js` | Stop real + rebuild |
| 7 | `dd_power_scores_cache` inconsistente | `estadisticas.js` | Renombrado a `yugioh_power_cache` |
| 8 | Pyro ausente en filtros avanzados | `buscador.js` | Añadido a `FILTER_DATA.monsterTypes` |
| 9 | External Score idéntico para todos los decks del meta | `estadisticas.js` | Cross-score N×N reemplaza al External Score del meta |
| 10 | Sin pantalla de carga al importar .ydk | `mideck.js` | Overlay en `parseYDK` |
| 11 | Nombre de rol duplicado (texto + input simultáneos) | `formacion.js` | Badge read-only + ✏️ activa input oculto |
| 12 | Botones clonar/borrar desaparecidos en rol | `formacion.js` | Restaurados ⧉ 🗑️ |
| 13 | Config no mostraba nada al cargar | `formacion.js` | `renderScoringSection` llamada pero no definida — error silencioso rompía `Config.render()` completo. Función implementada con sus 7 métodos save. |
| 14 | Había que abrir cada deck del meta para calcular scores | `estadisticas.js` | `recalculateAllMetaDeckScores` ahora async: descarga cartas faltantes de todos los decks automáticamente en 3 fases |
| 15 | Borrar "Cache" no limpiaba `metaDeckScores` en memoria | `formacion.js` | Añadido `Estadisticas.metaDeckScores = {}` |
| 16 | Borrar "Carpetas del Meta" dejaba cross-scores y deck scores | `formacion.js` | Limpia `yugioh_meta_decks`, `yugioh_meta_deck_scores`, `yugioh_cross_scores` |
| 17 | `segmentDescription` ausente en `NomenclatureAnalyzer` | `data.js` | Reintegrada; sin ella las capas L1–L5 no aplicaban multiplicadores |

---

## 10. API EXTERNA

**YGOProDeck** (sin auth, CORS libre)
```
Base: https://db.ygoprodeck.com/api/v7/
cardinfo.php?name=  cardinfo.php?id=  cardinfo.php?fname=
archetypes.php      cardsets.php
cardinfo.php        ← fetch completo usado por DefaultData._initGenesys()

Imágenes:
https://images.ygoprodeck.com/images/cards/{id}.jpg        → full
https://images.ygoprodeck.com/images/cards_small/{id}.jpg  → small
```

---

## 11. CLASES CSS CLAVE

| Clase | Archivo CSS | Propósito |
|-------|------------|-----------|
| `.tab-content` / `.active` | style_core | Secciones de pestañas |
| `.engines-sidebar` | style_mideck | Sidebar 4 tabs Mi Deck |
| `.cv-overlay` | style_core | Visor de carta |
| `#welcome-overlay.welcome-visible` | style_core | Panel bienvenida |
| `.role-panel` / `.role-panel-header` | style_config | Panel colapsable de rol y nomenclatura |
| `.scoring-layers-grid` | style_config | Grid de columnas L1/L2/L4/L5 |
| `.scoring-row` / `.scoring-grid` | style_config | Filas en Scoring G1/G2 |
| `.config-section-block` / `.config-block-title` | style_config | Bloques dentro de Scoring Avanzado |
| `.scoring-layer-col` / `.scoring-layer-entry` | style_config | Columnas y entradas de capas |
| `.asb-bar-track` / `.asb-bar-fill` | style_simuladores | Barras de score |
| `.analysis-g1g2-row` / `.analysis-g1g2-item` | style_simuladores | Bloque G1/G2 en análisis |
| `#meta-update-overlay` | inline en estadisticas.js | Overlay de carga del Actualizar Scores |

---

## 12. NOTAS PARA TRABAJAR EFICIENTEMENTE

**Para cambios puntuales:** sube solo la función afectada indicando objeto y método.

**Para CSS:** todos los ajustes responsive van exclusivamente en `responsive_style.css`.

**Archivos que NO necesitas subir salvo cambios:**
- `html2canvas_min.js` — librería externa
- `logger.js` — solo si cambia `TARGETS` o formato del reporte

**Variables CSS globales** (en `style_core.css`):
`--gold-color` · `--accent-color` · `--bg-card` · `--text-light` · `--danger-color` · `--spacing-sm/md/lg`

**Renombrados respecto a versiones anteriores:**
- `Duelo` → `DueloEnVivo` (simuladores.js)
- `dd_power_scores_cache` → `yugioh_power_cache`
- `Stats` vive dentro de `data.js`
- `default_data.js` carga DESPUÉS de `formacion.js` y ANTES de `logger.js`

**Diferencia clave entre los dos External Scores:**
- Deck activo → `Stats.calculateExternalScore()` → deck vs powerScoreCache
- Decks del meta → `calculateCrossExternalScores()` → N×N real

**Diferencia clave entre Peso de Rol y Poder Base:**
- `roleWeights` → escala 0.1–2.0 → Internal Score clásico (rendimientos decrecientes)
- `roleBasePower` → escala 0–10 → G1/G2 Score únicamente

**Regla de oro para añadir una sección a Config:**
1. Añadir el bloque HTML en `Config.render()` con el ID de sección
2. Definir `renderXxxSection()` que devuelve el HTML interno
3. Definir los métodos save que llaman `ConfigManager.getConfig()` / `saveConfig(cfg)`
4. Si hay datos nuevos en `defaultConfig`, añadir los getters correspondientes en `ConfigManager`
Si se omite el paso 2, `Config.render()` lanza un TypeError silencioso y la pestaña queda en blanco.