# DESTINY DRAW — REPORTE TÉCNICO
**Versión:** Post-consolidación | **Fecha:** Feb 2026
**Stack:** Vanilla JS (ES6 objects) · CSS modular · HTML5 SPA · YGOProDeck API · localStorage

---

## 1. ESTRUCTURA DE ARCHIVOS ACTUAL

### 1.1 HTML
```
index.html  — Shell única. Secciones vacías, JS llena el DOM en runtime.
```

### 1.2 JavaScript — 11 módulos (consolidados de 27)

**Orden de carga en index.html (crítico):**
```
core.js → data.js → buscador.js →
mideck.js → historial.js → estadisticas.js →
simuladores.js → simuladores-campo.js → formacion.js →
logger.js  (SIEMPRE AL FINAL — se carga post-módulos)
```

| Archivo | Objetos globales | Líneas | Absorbe |
|---------|-----------------|--------|---------|
| `logger.js` | `DDLogger` | ~250 | Logger no-invasivo. Se carga al final, envuelve todos los objetos globales post-`window.load`. **No modifica ningún módulo fuente.** API: `DDLogger.exportReport()`, `DDLogger.getLogs()`, `DDLogger.getStats()`, `DDLogger.enable/disable/clear()` |
| `core.js` | `Navigation`, `Shortcuts`, `ContentManager`, `switchTab` | 412 | navigation + utils + contentmanager |
| `data.js` | `ConfigManager`, `SpecialtyAnalyzer`, `NomenclatureAnalyzer` | 1433 | configmanager + specialty-analyzer + nomenclature-analyzer |
| `buscador.js` | `Buscador`, `CardViewer`, `Favoritas` | 1915 | buscador + cardviewer + favoritas |
| `mideck.js` | `Deck`, `Banlist`, `Engines` | 2971 | deck + banlist + engines |
| `historial.js` | `Matchups`, `Duelista` | 783 | matchups + duelista (duplicados eliminados) |
| `estadisticas.js` | `Estadisticas` | 2350 | — (solo, era demasiado grande) |
| `simuladores.js` | `Torneo`, `Duelo`, `Hipergeometria` | 2649 | torneo + duelo + hipergeometria |
| `simuladores-campo.js` | `ZonaPractica`, `Experimentacion` | 3228 | zonapractica + experimentacion |
| `formacion.js` | `Formacion`, `Meta`, `Config`, `Welcome`, `MusicPlayer`, `HelpPanel` | 3000 | formacion + meta + config + welcome + help |

**Nota:** `stats.js` y `winrate.js` no existían como archivos independientes — sus funciones estaban integradas en `estadisticas.js` y `deck.js` respectivamente.

### 1.3 CSS — 6 archivos (consolidados de 10)

**Orden de carga en index.html:**
```html
style_core.css → style_buscador.css → style_mideck.css →
style_simuladores.css → style_config.css → responsive_style.css
```

| Archivo | Líneas | Absorbe |
|---------|--------|---------|
| `style_core.css` | 1322 | style_global + style_welcome + style_cardviewer |
| `style_buscador.css` | 1017 | — (sin cambio estructural) |
| `style_mideck.css` | 2193 | — (sin cambio, límite de tamaño) |
| `style_simuladores.css` | 5844 | style_simuladores + style_estadisticas |
| `style_config.css` | 2164 | style_config + style_formacion + style_meta |
| `responsive_style.css` | 558 | **NUEVO** — todos los @media del proyecto extraídos |

**Todos los archivos CSS tienen sus `@media` eliminados** — viven exclusivamente en `responsive_style.css`.

---

## 2. ESTRUCTURA DEL DOM

| ID sección | Pestaña | Llenado por |
|-----------|---------|-------------|
| `#buscador-content` | Buscador | HTML estático + `Buscador.init()` |
| `#mideck-content` | Mi Deck | `Deck.render()` → `#deck-container` |
| `#estadisticas-content` | Estadísticas | `Estadisticas.render()` |
| `#simuladores-content` | Simuladores | `Torneo.init()` (lazy) |
| `#meta-content` | Meta | `Meta.init()` — **oculto en nav** (`display:none`) |
| `#formacion-content` | Formación | `Formacion.init()` |
| `#config-content` | Config | `Config.render()` |

---

## 3. LOCALSTORAGE — TODAS LAS CLAVES

| Clave | Módulo | Tipo | Descripción |
|-------|--------|------|-------------|
| `yugioh_config` | `ConfigManager` | Object | Config principal: roles, staples, specialties, nomenclature, pillars, metaMasters, metaLinks, formacionGames, formacionTopics, shortcuts, music |
| `yugioh_player_level` | `ConfigManager` | String | `'novato'\|'casual'\|'competitivo'\|'default'` — perfil elegido en Welcome y Config |
| `dd_player_profile` | `ContentManager` | String | `'novato'\|'casual'\|'competitivo'` — controla visibilidad de secciones |
| `dd_content_visibility` | `ContentManager` | Object | Override manual de visibilidad por sección ID |
| `dd_welcome_dismissed` | `Welcome` | String `'true'` | Panel solo reaparece al hacer Borrar Data |
| `yugioh_favoritas` | `Favoritas` | Object | `{cardId: {id,name,type,img,data}}` |
| `yugioh_engines` | `Engines` | Array | `[{id,name,cards,roles,notes,cover}]` |
| `yugioh_decks` | `Deck` | Object | Decks guardados `{nombre: {cards,notes}}` |
| `matchup_${deckName}` | `Matchups` | Array | `[{opponent,wins1st,losses1st,wins2nd,losses2nd,notes}]` |
| `yugioh_music_config` | `ConfigManager` | Object | `{enabled, volume, tracks:{novato,casual,competitivo}}` |
| `yugioh_meta_fallbacks` | `ConfigManager` | Object | Base64/URL imágenes fallback para Maestros |
| `yugioh_formacion_fallbacks` | `ConfigManager` | Object | Base64/URL imágenes fallback para Juegos |
| `yugioh_formacion_notes` | `Formacion` | Array | `[{title,content,date}]` |
| `yugioh_formacion_mastered` | `Formacion` | Array | IDs de temas dominados |
| `dd_power_scores_cache` | `Estadisticas` | Object | Cache de Power Scores calculados |
| `yugioh_meta_folders` | `Estadisticas` | Array | Carpetas del meta con decks .ydk importados |

---

## 4. DEPENDENCIAS ENTRE MÓDULOS

```
data.js (ConfigManager)  ←── buscador, mideck, historial, estadisticas,
                               simuladores, formacion, core (ContentManager)

core.js (ContentManager) ←── formacion (applyProfile al cambiar perfil)
                               navigation applyAll() en cada tab change

mideck.js (Engines)      ←── Favoritas.render() delega si _activeTab='favoritas'
                               Staples: usa campo imageUrl (NO img)

historial.js (Duelista)  ←── lee matchup_${deckName} de todos los decks
                               requiere Deck.getSavedDecks()

formacion.js (Meta)      ←── Sub-tabs Fuentes y Maestros de Formacion delegan a
                               Meta._renderFuentesSection() / _renderMaestrosSection()

formacion.js (Welcome)   ←── llama ConfigManager.savePlayerLevel() Y
                               ContentManager.applyProfile() al hacer enter()
```

---

## 5. FLUJOS CRÍTICOS

### 5.1 Cambio de pestaña
```
switchTab(name) → Navigation.showTab(name)
  → oculta todos .tab-content, activa #name-content
  → si mideck:            Engines.init()
  → si simuladores:       Torneo.init()
  → si formacion:         Formacion.init()
  → si meta:              Meta.init()
  → ContentManager.applyAll()
  → Estadisticas.updateFloatingWidgetVisibility()
```

### 5.2 Selección de perfil (Welcome)
```
Welcome.enter(tabName, levelKey)
  → ContentManager.applyProfile(levelKey)  → 'dd_player_profile'
  → ConfigManager.savePlayerLevel(levelKey) → 'yugioh_player_level'
  → MusicPlayer.setTrack(cfg.tracks[levelKey])
  → Welcome.dismiss() → localStorage 'dd_welcome_dismissed' = 'true'
  → Navigation.showTab(tabName)
```

### 5.3 Selección de perfil (Config)
```
Config.selectPlayerLevel(levelKey)
  → ConfigManager.savePlayerLevel(levelKey)
  → ContentManager.applyProfile(levelKey)
  → MusicPlayer.setTrack(...)
  → Config.render()
```

### 5.4 Borrar Todo
```
Config.borrarTodo()
  → localStorage.clear() completo
  → Welcome.dismissed = false
  → localStorage.removeItem('dd_welcome_dismissed')
  → reconstruye config vacía
  → Deck.render(), Estadisticas.render(), Favoritas.render()
  → Welcome.init() → muestra panel de nuevo
```

### 5.5 Render sidebar Mi Deck
```
Engines._renderSidebar()
  → lee _activeTab (engines|decks|staples|favoritas)
  → staples: _renderStaplesItems() → imageUrl (NO img)
  → favoritas: _renderFavoritasItems() → Favoritas.getAll()
```

---

## 6. SUB-TABS POR PESTAÑA

### Mi Deck (`mideck.js`)
```
Sub-tabs:  Decklist | Construcción
Sidebar (2×2 grid):  Engines | Decks | Staples | Favoritas
```

### Formación (`formacion.js`)
```
Sub-tabs:  📓 Apuntes | 📖 Temas | 🎮 Juegos | 🔗 Fuentes | 🎓 Maestros
           (Fuentes y Maestros delegan a Meta._renderFuentesSection/MaestrosSection)
```

### Simuladores (`simuladores.js`)
```
Sub-tabs:  Mulligan | Winrate | Torneo | Duelo | Experimentación | Práctica
```

### Estadísticas (`estadisticas.js`)
```
Secciones (IDs para Shortcuts y ContentManager):
  deck-stats-sec | deck-analysis-sec | winrate-sec | top-tier-sec |
  meta-decks-sec | power-scores-sec | counter-cards-sec | export-sec | duelista-sec
```

---

## 7. BUGS RESUELTOS EN ESTA SESIÓN

| 4 | `logger.js` y `parallax.js` referenciados pero inexistentes | `index.html` | Eliminados del orden de carga; nuevo `logger.js` creado (DDLogger) |
|---|-----|---------|----------|
| 1 | `_calcWR` y `_getAllMatchupRecords` duplicados 3 veces | `duelista.js` → `historial.js` | Eliminadas las 2 copias extra |
| 2 | `yugioh_player_level` y `dd_player_profile` desincronizados | `welcome.js` + `config.js` | `enter()` ahora escribe ambas claves |
| 3 | Welcome Panel reaparecía al recargar | `welcome.js` | `init()` lee `dd_welcome_dismissed`, `dismiss()` la escribe |

---

## 8. API EXTERNA

**YGOProDeck** (sin auth, CORS libre)
```
Base: https://db.ygoprodeck.com/api/v7/
cardinfo.php?name=  cardinfo.php?id=  cardinfo.php?fname=
archetypes.php      cardsets.php

Imágenes:
https://images.ygoprodeck.com/images/cards/{id}.jpg        → full
https://images.ygoprodeck.com/images/cards_small/{id}.jpg  → small
```

---

## 9. CLASES CSS CLAVE

| Clase | Archivo CSS | Propósito |
|-------|------------|-----------|
| `.tab-content` / `.active` | style_core | Secciones de pestañas |
| `.engines-sidebar` | style_mideck | Sidebar 4 tabs Mi Deck |
| `.eng-tab-btn.eng-tab-active` | style_mideck | Tab activo sidebar |
| `.mideck-subtabs-nav` / `.mideck-subtab-btn.active` | style_mideck | Sub-tabs Decklist/Construcción |
| `.deck-actions` (grid 3×2) | style_mideck | Botones de acción del deck |
| `.form-subnav-btn.active` | style_config | Tab activo Formación |
| `.cv-overlay` / `.card-viewer-overlay` | style_core | Visor de carta |
| `#welcome-overlay.welcome-visible` | style_core | Panel bienvenida visible |
| `.cm-profile-btn.cm-profile-active` | style_core | Perfil activo en Config |

---

## 10. NOTAS PARA TRABAJAR EFICIENTEMENTE

**Para cambios puntuales:** sube solo la función afectada indicando objeto y método. Ej: `Engines._renderStaplesItems` en `mideck.js`.

**Para CSS:** todos los ajustes responsive van exclusivamente en `responsive_style.css`. Los 5 archivos principales no tienen `@media`.

**Archivos que NO necesitas subir salvo cambios:**
- `html2canvas_min.js` — librería externa
- `logger.js` — solo si cambia la lista `TARGETS` o el formato del reporte

**Variables CSS globales** (definidas en `style_core.css`):
`--gold-color` · `--accent-color` · `--bg-card` · `--text-light` · `--danger-color` · `--spacing-sm/md/lg`