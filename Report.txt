# DESTINY DRAW — REPORTE TÉCNICO
**Versión del reporte:** v9.0 (estado actual, sin historial de cambios)
**Stack:** Vanilla JS (ES6 objects) · CSS modular · HTML5 SPA · YGOProDeck API · Yugipedia API · localStorage

> Este reporte describe el estado ACTUAL de la app: qué existe, cómo funciona a nivel
> de flujo de código, y qué hace cada pestaña para el usuario. No contiene changelog
> ni historial de bugs resueltos — solo lo que el próximo programador necesita para
> seguir construyendo sin romper nada.
>
> **Aviso de auditoría (v7.0):** esta pasada fue una **auditoría estructural** (no
> línea-por-línea) sobre `core.js`, `buscador.js`, `formacion.js`, `mideck.js`,
> `simuladores.js`, `simuladores-campo.js`, `estadisticas.js`, `logger.js`, `lobby.js`
> y `decklists.js`: conteo de líneas real, objetos globales, métodos de primer nivel,
> secciones de Config, claves de `localStorage` y el `index.html` real. Confirma o
> corrige todo lo que la v6.0 marcaba como "no verificado"/"probable" para Lobby,
> Combos, Gauntlet, Counters (CounterSim), TabIntro y decklists.js, y saca a la luz
> features enteras que no estaban ni mencionadas (ver secciones 4.3, 4.5 y 4.7).
> **Lo que esta pasada NO cubre:** la lógica interna línea-por-línea de los métodos
> nuevos más grandes — sobre todo dentro de `Combos` y `ZonaPractica`. Donde el
> reporte dice "confirmado que existe" pero no "confirmado el flujo exacto", conviene
> leer el archivo real antes de tocarlo a fondo. Tu Experiencia con el Deck e
> Historial de Versiones (`mideck.js`) siguen sin flujo trazado en detalle.
> **Verificado directamente contra el `index.html` actual en esta pasada** (no es
> inferencia): orden real de carga de `<script>`, estructura del `<nav>`, IDs de
> sección y footer.
>
> **Aviso de auditoría (v8.0):** pasada dirigida sobre `mideck.js`, `style_mideck.css`
> e `index.html`, a raíz de cambios reportados en Mi Deck (vista Detallada/Resumida
> del Decklist, ajustes de Side Deck). `index.html` y `mideck.js` resultaron ser
> **byte-idénticos** a los ya auditados en v7.0 (mismos límites de línea exactos para
> `Deck`/`Combos`/`Banlist`/`Engines`) — no hubo cambios de código nuevos entre
> pasadas. Lo que sí cambió es la cobertura del reporte: la v7.0 no había bajado al
> nivel de detalle de la Vista Resumida/Detallada ni de las reglas de orden/tope del
> Side Deck, que ya existían en el archivo y quedan documentadas recién ahora (ver
> 4.3). `style_mideck.css` sí se leyó por primera vez en esta pasada — confirma que
> las clases de Combos, Tu Experiencia con el Deck, Complejidad y Optimización
> flaggeadas como "no confirmado" en v6.0 sí están ahí, pero destapa que `.tdp-*`
> (usada por `mideck.js` para el panel de "Ver Deck" y ahora también para la Vista
> Resumida) **no** tiene ninguna regla en este archivo — vive en otro CSS no leído
> todavía, o está sin estilos.

> **Aviso de auditoría (v9.0):** pasada sobre el lote completo de JS/CSS subido
> directamente (no vía proyecto): `buscador.js`, `core.js`, `data.js`,
> `decklists.js`, `default_data.js`, `estadisticas.js`, `formacion.js`,
> `historial.js`, `lobby.js`, `logger.js`, `mideck.js`, `simuladores.js`,
> `simuladores-campo.js` y los 6 CSS. Varios archivos crecieron sustancialmente
> desde la v8.0 sin que el reporte lo reflejara — en particular `lobby.js`
> (247→578 líneas: suma dos módulos globales nuevos, `TourGate` y `AppTour`,
> no documentados hasta ahora) y `decklists.js` (105→659 líneas: pasó de 10 a
> **204** decklists embebidas). Se confirma y documenta por primera vez un
> sistema de scoring completo que ya existía en `data.js`/`estadisticas.js` pero
> nunca se había descrito (**Counter-Deck Score**, ver 5.3bis), el panel global
> de Ayuda/FAQ (`HelpPanel`, ya listado en la tabla de módulos desde v7.0 pero
> nunca desarrollado en el cuerpo del reporte), el botón flotante "Guardar
> Deck" de `mideck.js`, y se resuelve el punto abierto del flujo 8.1 sobre dónde
> se dispara `TabIntro.maybeShow()`. `.tdp-*` (gap de la v8.0) se confirma que
> vive en `style_simuladores.css`, no en `style_mideck.css`. **Esta pasada NO
> fue línea-por-línea sobre los módulos ya confirmados a fondo en v6.0-v8.0**
> (Combos, Gauntlet, CounterSim, ZonaPractica, Experiencia con el Deck) — se
> enfocó en detectar features nuevas/no registradas vía inventario de objetos
> globales, claves de `localStorage` y crecimiento de línea por archivo. Donde
> no se indica lo contrario, las secciones de v6.0-v8.0 siguen vigentes.

---

## 1. ESTRUCTURA DE ARCHIVOS

### 1.1 HTML
```
index.html — Shell SPA. El Buscador trae su HTML estático completo (barra de
              búsqueda, filtros, sets); el resto de secciones (#lobby-content,
              #mideck-content, #estadisticas-content, #simuladores-content,
              #meta-content, #formacion-content, #config-content) están vacías
              (o con placeholder) y el JS llena el DOM en runtime.
Footer real:  "VERSION 10.4.9" (independiente del número de este reporte — **corregido en v9.0**, la v8.0 decía "10.2.9").
```

**Detalle verificado directo del `index.html` actual, útil para no asumir de más:**
- Todos los botones de `.nav-tabs` usan `onclick="switchTab('xxx')"` **excepto** el de
  Config, que solo trae `data-tab="config"` sin `onclick`. Probablemente se apoya en
  delegación por `data-tab` desde `Navigation` (igual que los demás, como respaldo),
  pero no está confirmado en esta pasada — si algún día "Config" deja de cambiar de
  pestaña al hacer clic, empezar a revisar por acá.
- El `<li>` de la pestaña Meta tiene `style="display:none"` inline — coincide con que
  Meta no está en `Navigation.tabs` y se activa solo internamente.
- `#config-content` trae un placeholder estático `"Contenido de configuración
  (pendiente de implementación)"` en el HTML — es cosmético, se sobreescribe apenas
  corre `Config.render()`.
- `Modules/pdf.min.js` se carga como `<script>` normal en el `<head>`; `pdf.worker.min.js`
  **no** tiene su propio `<script>` tag — se asume que `pdfjsLib.GlobalWorkerOptions.workerSrc`
  apunta a él en runtime dentro de `mideck.js`.
- **Resuelto en esta pasada:** `Modules/decklists.js` **sí** está cargado — el `<script>`
  tag existe entre `default_data.js` y `logger.js`. La discrepancia que la v6.0 marcaba
  sobre este módulo ya no aplica (ver fila propia en la tabla de 1.2).

### 1.2 JavaScript — 13 módulos + 1 módulo de datos por defecto

**Orden de carga real en `index.html` (confirmado, crítico):**
```
core.js → data.js → buscador.js → lobby.js →
mideck.js → historial.js → estadisticas.js →
simuladores.js → simuladores-campo.js → formacion.js →
default_data.js → decklists.js → logger.js  (SIEMPRE AL FINAL)
```

| Archivo | Objetos globales | Líneas (confirmado en esta pasada) | Descripción |
|---------|-----------------|---------------------------|--------------|
| `core.js` | `Navigation`, `TabIntro`, `Shortcuts`, `ContentManager`, `switchTab` | 692 (antes 666) — crecimiento menor: `Navigation.showTab` ahora también llama `Deck.updateSaveFloatingBtnVisibility` y `Lobby.init` (ver 8.1) | Navegación entre pestañas, intro por pestaña, accesos rápidos, visibilidad por perfil, tema de fondo |
| `data.js` | `ConfigManager`, `Stats`, `SpecialtyAnalyzer`, `NomenclatureAnalyzer` | 2317 (antes 2311) — incluye `Stats.calculateCounterDeckScore` (ver 5.3bis), no documentado hasta v9.0 | Config global, motor de Scoring (Internal, G1/G2, External, Counter-Deck, RPS), analizadores de carta |
| `buscador.js` | `Buscador`, `CardViewer`, `Favoritas` | 2480 (antes 2477) — sin cambios relevantes | Búsqueda, visor de carta, favoritas, imagen de portada configurable, Lore (Yugipedia) |
| `lobby.js` | `Lobby`, `TourGate`, `AppTour` | 578 (antes 247) — el objeto `Lobby` en sí no cambió; el crecimiento es por dos módulos nuevos agregados al final del archivo (ver 4.1bis) | Pestaña de inicio: Lo Nuevo, Sugerencias (colapsable), aviso de actualización — más `TourGate` (orquestador de orden de paneles de bienvenida) y `AppTour` (tour guiado de onboarding por pestañas) |
| `mideck.js` | `Deck`, `Combos`, `Banlist`, `Engines` | 8612 (antes 8321) — crecimiento por el botón flotante "💾 Guardar Deck" y `Deck.saveNotes()` (ver 4.3), no documentados hasta v9.0 | Deck activo, Optimización, Complejidad, Experiencia con el Deck, Línea de Combos, Banlist/Genesys, Importar PDF/.ydk, sidebar, guardado rápido |
| `historial.js` | `Matchups`, `Duelista` | 930 — sin cambios | Historial por rival y Nivel como Piloto del deck activo |
| `estadisticas.js` | `Estadisticas` | 2673 — sin cambios | Scores del deck activo y del meta, Top Tier, carpetas del meta, Poder de Cartas |
| `simuladores.js` | `Winrate`, `Torneo`, `DueloEnVivo`, `Hipergeometria`, `CounterSim`, `Gauntlet` | 4330 — sin cambios de línea, pero se confirma en v9.0 que `Hipergeometria` tiene 3 sub-modos (Estándar/Mis Decks/Prueba Mulligan, ver 4.5), no documentados antes | Simuladores de testing y práctica competitiva |
| `simuladores-campo.js` | `ZonaPractica`, `Experimentacion` | 3641 (antes 3617) — crecimiento menor, sin features nuevas de fondo detectadas en esta pasada | Simulación de campo por zonas y lienzo libre |
| `formacion.js` | `Formacion`, `Meta`, `TestDuelo`, `Config`, `Welcome`, `MusicPlayer`, `HelpPanel` | 7944 (antes 7858) — el crecimiento corresponde al contenido completo de `HelpPanel` (panel de Ayuda/FAQ global, ver 4.8), listado en esta tabla desde v7.0 pero nunca descrito en el cuerpo del reporte hasta v9.0 | Contenido educativo, Config completo, bienvenida, música, ayuda/FAQ global, Tests de Duelo custom |
| `default_data.js` | `DefaultData` | 1229 (antes 1228) — sin cambios relevantes | Inyección única de datos por defecto. Parchea `Config.resetToDefault` |
| `decklists.js` | `DecklistsData` | 659 (antes 105) — **confirmado en esta pasada: 204 decklists**, no 10 | Repositorio de 204 decklists `.ydk` embebidas, cada una con su propio campo `level` (`'Basico'\|'Intermedio'\|'Competitivo'`) para "🃏 Primeros Decks" en Formación. `getGroupedByLevel()` agrupa y ordena alfabéticamente dentro de cada nivel (ver 4.6) |
| `logger.js` | `DDLogger` | 368 (antes 365) — `TARGETS` se amplió y quedó al día salvo `TourGate`/`AppTour` (ver nota debajo de esta tabla) | Logger no-invasivo. `exportReport()`, `getLogs()`, `getStats()`, `enable/disable/clear()` |

**Nota:** `Stats` vive dentro de `data.js`. `Winrate`/`DueloEnVivo` son los objetos correctos en `simuladores.js` (el antiguo `Duelo` ya no existe). `default_data.js` carga después de `formacion.js`, antes de `decklists.js`, y `logger.js` sigue siempre al final.

**✅ Bug de `logger.js` — RESUELTO, confirmado en v9.0:** `TARGETS` ya cubre `Lobby`, `Combos`, `CounterSim`, `Gauntlet`, `TabIntro`, `TestDuelo` y el nombre correcto `'DueloEnVivo'` (ya no aparece el obsoleto `'Duelo'`). Lista completa actual: `Navigation, Shortcuts, ContentManager, TabIntro, ConfigManager, SpecialtyAnalyzer, NomenclatureAnalyzer, Buscador, CardViewer, Favoritas, Deck, Combos, Banlist, Engines, Matchups, Duelista, Estadisticas, Winrate, Torneo, DueloEnVivo, Hipergeometria, CounterSim, Gauntlet, DecklistsData, ZonaPractica, Experimentacion, Formacion, Meta, Config, Welcome, MusicPlayer, HelpPanel, TestDuelo, DefaultData, Lobby`. `DecklistsData` sigue sin aplicar (es solo data, sin métodos que interceptar). **⚠️ Gap nuevo confirmado en v9.0:** `TourGate` y `AppTour` (ver 4.1bis, agregados a `lobby.js` después de que se armó la lista de `TARGETS`) **no** están en `TARGETS` — no se auditan ni aparecen en `exportReport()`.

### 1.3 CSS — 6 archivos

**Orden de carga en `index.html` (confirmado):**
```
style_core.css → style_buscador.css → style_mideck.css →
style_simuladores.css → style_config.css → responsive_style.css
```

| Archivo | Líneas (última medición) | Absorbe |
|---------|---------------------------|---------|
| `style_core.css` | 1614 (antes 1326) — crecimiento confirmado en v9.0, coincide con las clases de `.help-panel-*`/`.faq-*` (HelpPanel, ver 4.8) y el grueso de `.lobby-*`/`.tabintro-*` | style_global + style_welcome + style_cardviewer + Ayuda/FAQ |
| `style_buscador.css` | 1383 (antes ~1045) — confirmado en v9.0 | — (+ `.cv-archetype-link`, `.cv-release-block`, `.cv-card-id`, `.cv-contribution-*`, `.cv-role-*`, y probablemente el bloque de Lore de Yugipedia, no confirmado) |
| `style_mideck.css` | 3569 (antes 3515) — confirmado en v9.0. Ya incluye `.combo-*` (Línea de Combos, editor + objetivo + ramificaciones), `.exp-*` (Tu Experiencia con el Deck), `.dl-view-*`/`.dv-*`/`.dzc-*` (Vista Detallada/Resumida y chips de zona, ver 4.3). **Gap del `.tdp-*` RESUELTO en v9.0:** esas clases no viven acá — ver fila de `style_simuladores.css` | — (incluye `.opt-*` de Optimización — 165 ocurrencias confirmadas —, `.cxd-*` de Complejidad del Deck — 26 confirmadas) |
| `style_simuladores.css` | 6518 (antes 6019) — **confirmado en v9.0 que acá vive `.tdp-*`** (14 ocurrencias, panel "Ver Deck" de Matchups, ver sección 11). Sigue **desactualizado** para `CounterSim`/`Gauntlet`: no se detectaron clases con esos nombres pese al crecimiento del archivo — es posible que reutilicen clases genéricas existentes en vez de un prefijo propio; confirmar antes de asumir que están sin estilos | style_simuladores + style_estadisticas |
| `style_config.css` | 2646 (antes 2292) — confirmado en v9.0 | style_config + style_formacion + style_meta + clases de Scoring G1/G2 + subconjunto de `.tabintro-*`/`.lobby-*` (sección "Intro de Pestañas") |
| `responsive_style.css` | 739 (antes 611) — confirmado en v9.0 | Todos los `@media` del proyecto |

**Todos los `@media` viven exclusivamente en `responsive_style.css`** (los 5 archivos temáticos no traen `@media` propio — última verificación confirmó 0 ocurrencias en ellos).

> **Actualizado en v9.0:** los 6 archivos CSS sí fueron leídos directamente en esta
> pasada (medición de línea real vía `wc -l`, más grep dirigido de clases clave) —
> las cifras de la tabla de arriba ya no son la medición de v6.0. Sigue sin hacerse
> una lectura línea-por-línea de cada regla; lo confirmado es el conteo total y la
> ubicación real de los grupos de clases mencionados arriba (`.tdp-*`, `.help-panel-*`,
> `.lobby-*`/`.tabintro-*`).

---

## 2. ESTRUCTURA DEL DOM

| ID sección | Pestaña | Llenado por |
|-----------|---------|-------------|
| `#lobby-content` | Lobby | `Lobby.render()` |
| `#buscador-content` | Buscador | HTML estático + `Buscador.init()` |
| `#mideck-content` | Mi Deck | `Deck.render()` → `#deck-container` |
| `#estadisticas-content` | Estadísticas | `Estadisticas.render()` |
| `#simuladores-content` | Simuladores | `Torneo.init()` (lazy) |
| `#meta-content` | Meta | `Meta.init()` — fuera de `Navigation.tabs`, se activa internamente |
| `#formacion-content` | Formación | `Formacion.init()` |
| `#config-content` | Config | `Config.render()` |

```javascript
Navigation.tabs = ['lobby', 'buscador', 'mideck', 'estadisticas', 'simuladores', 'formacion', 'config']
Navigation.currentTab (default) = 'lobby'
```
`meta` está oculto en el nav HTML (`style="display:none"` en el `<li>`) y no figura en `Navigation.tabs`; se activa vía `Meta.init()` o `switchTab('meta')` desde otra sección.

---

## 3. LOCALSTORAGE — TODAS LAS CLAVES

| Clave | Módulo | Tipo | Descripción |
|-------|--------|------|-------------|
| `yugioh_config` | `ConfigManager` | Object | Config principal: roles, roleConditions, roleWeights, staples, specialties, nomenclature, pillars, pillarRPS, g1g2Roles, roleBasePower, reliabilityTable, scoringLayers, metaMasters, metaLinks, formacionGames, formacionTopics, shortcuts, music |
| `yugioh_player_level` | `ConfigManager` | String | `'novato'\|'casual'\|'competitivo'\|'default'` |
| `dd_player_profile` | `ContentManager` | String | Controla visibilidad de secciones por perfil |
| `dd_content_visibility` | `ContentManager` | Object | Override manual de visibilidad por sección/componente |
| `dd_bg_theme` | `ContentManager` | String | Tema de fondo elegido |
| `dd_tabintro_disabled` | `TabIntro` | Array/Object | Pestañas con la intro desactivada de forma permanente (checkbox "No volver a mostrar") |
| `dd_tabintro_shown_${tab}` *(nuevo, confirmado)* | `TabIntro` | **sessionStorage**, String `'true'` | Marca que la intro de esa pestaña ya se mostró en esta sesión de navegador (se resetea al recargar). No es `localStorage` — no aparece en Exportar/Importar Data ni en Zona de Borrado. |
| `dd_welcome_dismissed` | `Welcome` | String `'true'` | Panel reaparece solo al Borrar Data |
| `dd_update_seen` | `Lobby` | String/Number | Última versión de aviso de actualización ya vista |
| `dd_lobby_latest_cache` *(nuevo, confirmado v9.0)* | `Lobby` | Object `{ts, cards}` | Caché persistida de "🆕 Lo Nuevo" — vigente 24h (`Lobby.CACHE_MS`), evita refetch a la API en cada reload de página |
| `dd_apptour_seen` *(nuevo, confirmado v9.0)* | `AppTour` | String `'true'` | Marca que el usuario ya completó el tour guiado de onboarding — mientras no esté en `'true'`, bloquea también todas las intros individuales de `TabIntro` (ver 4.1bis) |
| `dd_buscador_sidebar_img` | `Buscador` | String | Imagen de portada elegida por el usuario |
| `yugioh_img_quality` *(nuevo, confirmado)* | `Buscador` | String | Calidad de imagen de carta preferida (`Buscador.setImgQuality`) |
| `yugioh_favoritas` | `Favoritas` | Object | `{cardId: {id,name,type,img,data}}` |
| `yugioh_engines` | `Engines` | Array | `[{id,name,cards,roles,notes,cover}]` |
| `deck_${nombre}` (prefijo, 1 clave por deck) | `Deck` | Object | `{cards, notes, savedAt, uid, versions[]}`. `getSavedDecks()` escanea `localStorage` por el prefijo `deck_`. No existe un único objeto `yugioh_decks` — esa clave es referencia legacy usada solo como limpieza de seguridad en Borrar Data / Exportar Reporte. `versions[]` (Historial de Versiones) se guarda dentro de esta misma clave. |
| `dd_decklist_view_mode` *(nuevo, confirmado en v8.0)* | `Deck` | String | `'detallada'\|'resumida'` — modo de vista del Decklist (Main/Extra/Side), default `'resumida'` en un deck nuevo. Es una preferencia global de UI, no por deck. |
| `combos_${deckName}` | `Combos` | Object (confirmado que existe y se usa; estructura interna no auditada línea por línea) | Líneas de combo del deck: zonas (mano/campo/cementerio/baneadas), starters/Boss Monster detectados o manuales, choke points con frecuencia, restricciones por severidad, dependencias entre pasos, "copia de" (uso alternativo de una copia), interacciones automáticas + manuales, endboard con función por carta, objetivo del combo, ramificaciones |
| `experiencia_${deckName}` | `Deck` | Object | Perfil cualitativo: dificultad, estrategia, variante, non-engine slots, manos muertas, composición, cartas destacadas, sets |
| `optimization_${nombre}` | `Deck` | Object | Datos de 🎯 Optimización: `{sessions:[{id,date,label,rounds:[{id,orden,resultado,tipoVictoria,tipoDerrota,presionTiempo,comboCompleto,brick,starter,extenders,handtraps,boardbreakers,rompioBoard,negoJugada,rivalRompio,notas}]}]}`. Migración automática desde formato viejo `records[]`. |
| `complejidad_${nombre}` | `Deck` | Object | `{answers:[8 valores 1-3], aprender, dominar, total, evaluatedAt}` — ver sección 7.1 |
| `matchup_${deckName}` | `Matchups` | Array | `[{id,opponentName,wins1st,losses1st,wins2nd,losses2nd,notes,cardData,createdAt}]`. `cardData` opcional (decklist del rival vía `.ydk`), visible con "Ver Deck". |
| `pz_states_${deckName}` | `ZonaPractica` | — | Estados guardados del campo de práctica |
| `gauntlet_${tipo}_${nombre}` | `Gauntlet` | — (confirmado el patrón de clave vía `_storageKey()`; estructura interna no auditada) | Pruebas y config de un pool testeado contra el meta |
| `yugioh_gauntlet_rankings` / `yugioh_gauntlet_templates` | `Gauntlet` | — | Ranking global y plantillas reutilizables de pruebas |
| `yugioh_music_config` | `ConfigManager` | Object | `{enabled, volume, tracks:{novato,casual,competitivo}}` |
| `yugioh_meta_fallbacks` | `ConfigManager` | Object | Imágenes fallback para Maestros |
| `yugioh_formacion_fallbacks` | `ConfigManager` | Object | Imágenes fallback para Juegos |
| `yugioh_formacion_notes` | `Formacion` | Array | `[{title,content,date}]` |
| `yugioh_formacion_mastered` | `Formacion` | Array | IDs de temas dominados |
| `yugioh_estilo_juego` *(nuevo, confirmado)* | `Formacion` | Object | Resultado guardado del quiz "🧭 Tu Estilo" (ejes de preferencia → arquetipo afín) |
| `yugioh_personaje_resultado` *(nuevo, confirmado)* | `Formacion` | Object | Resultado guardado del quiz "🎭 Tu Personaje" |
| `yugioh_custom_tests` *(nuevo, confirmado)* | `TestDuelo` | Array | Tests de Duelo personalizados (Teóricos y Prácticos), creados en Config → 🧪 Test de Duelo o importados desde `.txt` |
| `yugioh_power_cache` | `Estadisticas` | Object | Cache de Power Scores calculados |
| `yugioh_cross_scores` | `Estadisticas` | Object | Cross-scores entre decks del meta |
| `yugioh_meta_decks` | `Estadisticas` | Array | Decks del meta importados (.ydk) |
| `yugioh_meta_deck_scores` | `Estadisticas` | Object | Scores calculados de cada deck del meta |
| `yugioh_meta_card_library` | `Estadisticas` | Object | Librería de cartas del meta (cache local de la API) |
| `dd_report_counter` | **`Config`** *(corregido — antes atribuido a `DDLogger`)* | Number | Contador de reportes enviados desde "📧 Reportar Error" (`Config.enviarReporte`, ver flujo 8.13). **No** tiene relación con `DDLogger.exportReport()` — son dos exports .txt distintos y sin conexión entre sí. |

**Flags de DefaultData** (sobreviven a Borrar Data, se eliminan solo con Restaurar Config): `dd_default_decks_loaded`, `dd_default_notes_loaded`, `dd_default_staples_loaded`, `dd_default_specialties_loaded`, `dd_default_engines_loaded`, `dd_default_games_loaded`, `dd_default_masters_loaded`, `dd_default_genesys_loaded`.

**Nota:** `g1g2Roles`, `roleBasePower`, `reliabilityTable` y `scoringLayers` viven dentro de `yugioh_config`. Export, Import, Borrar (Config), Reporte y Restaurar Config los cubren automáticamente.

### Zona de Borrado selectivo (16 categorías)

**Confirmado en esta pasada directo contra el HTML real de `Config.render()`:** son exactamente 16 checkboxes con `data-key`, coincide con la tabla de abajo.

| data-key | Claves eliminadas | En memoria |
|----------|------------------|------------|
| `decks` | prefijo `deck_` (+ limpieza legacy de `yugioh_decks`) | `Deck.cards = {}` |
| `engines` | `yugioh_engines` | — |
| `matchups` | prefijos `matchup_` | — |
| `optimizacion` | prefijos `optimization_` y `complejidad_` | — |
| `winrates` | `yugioh_winrates`, `pz_winrate_standalone` | — |
| `favoritas` | `yugioh_favoritas` | — |
| `torneo` | `yugioh_torneo_actual` | — |
| `practica` | prefijos `pz_states_` | — |
| `cache` | `yugioh_power_cache`, `yugioh_cross_scores`, `yugioh_meta_deck_scores` | `powerScoreCache=null`, `crossScores={}`, `metaDeckScores={}` |
| `meta_library` | `yugioh_meta_card_library` | `metaCardLibrary={}` |
| `meta_folders` | `yugioh_meta_folders`, `yugioh_meta_decks`, `yugioh_meta_deck_scores`, `yugioh_cross_scores` | `metaDecks={}`, `metaFolders=[]`, `metaDeckScores={}`, `crossScores={}` |
| `formacion` | `yugioh_formacion_notes`, `yugioh_formacion_mastered` | — |
| `banlist` | `yugioh_banlist_data` | — |
| `config` | `yugioh_config` | — |
| `perfil` | `yugioh_player_level`, `dd_player_profile`, `dd_content_visibility`, `dd_welcome_dismissed` | — |
| `fallbacks` | `yugioh_meta_fallbacks`, `yugioh_formacion_fallbacks` | — |

> ⚠️ **Gap a confirmar (ampliado en v9.0):** `combos_${deckName}`, `experiencia_${deckName}`, `gauntlet_${tipo}_${nombre}`, `yugioh_gauntlet_rankings/templates`, `dd_bg_theme`, `dd_tabintro_disabled`, `dd_update_seen`, `dd_buscador_sidebar_img`, `yugioh_img_quality`, `yugioh_estilo_juego`, `yugioh_personaje_resultado`, `yugioh_custom_tests`, `dd_report_counter`, `dd_decklist_view_mode` — y ahora también las dos claves nuevas confirmadas en esta pasada: `dd_lobby_latest_cache`, `dd_apptour_seen` — **no aparecen mapeados a ninguna de las 16 categorías** de arriba. O están cubiertos implícitamente por una categoría existente (ej. `combos_` bajo `decks`) y falta documentarlo, o son datos huérfanos que Borrar Data no limpia. Confirmar antes de asumir que "Borrar Todo" los alcanza.

---

## 4. PESTAÑAS Y HERRAMIENTAS

### 4.1 🏠 Lobby
Pestaña de inicio, activa por defecto. **Confirmado contra código en esta pasada** (antes era solo prosa funcional, sin relectura).
- **🆕 Lo Nuevo:** tira horizontal con las últimas 10 cartas lanzadas en TCG u OCG en el último mes, fusionadas por fecha (si salió en ambos formatos, se muestra una sola vez con la fecha más temprana). Clic abre `CardViewer`. **Corregido en v9.0:** la caché real es de **24 horas** (`Lobby.CACHE_MS`) y persiste en `localStorage` (`dd_lobby_latest_cache`, ver 3), no solo en memoria por 30 min — sobrevive a un reload de página.
- **💡 Sugerencias:** collage de 5 tarjetas aleatorias (pool de 10) que invitan a otras secciones (Simuladores, Optimización, Matchups, Buscador por arquetipo, Config → Scoring, Complejidad del Deck, Banlist, Formación, Importar Deck, Estadísticas). "🔀 Otras sugerencias" re-randomiza. Clic navega directo. Toda la sección es **colapsable** (`Lobby.toggleSugerencias()`, flecha ▼/▶) — "Lo Nuevo" no lo es.
- **Aviso de actualización:** overlay con las mejoras de la versión actual (`Lobby.UPDATE_NOTES`), una sola vez por versión, se descarta con "Entendido".

### 4.1bis `TourGate` y `AppTour` *(nuevos en `lobby.js`, confirmados en esta pasada — no documentados hasta ahora)*
Dos módulos globales añadidos al final de `lobby.js`, sin modificar `Lobby`/`Welcome`/`TabIntro` — solo observan su estado por DOM/`localStorage`.
- **`TourGate`:** orquesta el orden de los paneles de bienvenida al primer uso: Aviso de Actualización de Lobby → Panel de Bienvenida (`Welcome`) → App Tour → recién ahí `TabIntro` queda habilitado. `_watchUpdateOverlay()`/`_watchWelcomeOverlay()` hacen polling cada 300ms sobre el DOM/`dd_welcome_dismissed` hasta que ambos overlays se cerraron; `waitUntilReady(cb)` dispara el callback cuando `ready()` es `true`.
- **`AppTour`:** tour guiado de onboarding, una sola vez por usuario (`localStorage: dd_apptour_seen`). `maybeStart()` se dispara en `window.addEventListener('load', ...)` y espera a `TourGate` antes de arrancar. Recorre las 7 pestañas principales (`steps[]`) navegando con `switchTab()`, mostrando 1-3 "slides" de texto por pestaña con palabras clave resaltadas (`_highlightHTML`, regex sobre `slide.keywords`). Panel fijo abajo de pantalla con dots de progreso, botones Atrás/Siguiente/Saltar; `finish()` persiste `dd_apptour_seen='true'` y destruye el DOM del tour. **Efecto colateral importante:** parchea `TabIntro.maybeShow` (IIFE `gateTabIntro()` al final del archivo) para que no dispare ninguna intro de pestaña hasta que `dd_apptour_seen === 'true'` — si el AppTour nunca se completa, las intros individuales de `TabIntro` quedan bloqueadas indefinidamente.
- **Gap confirmado:** ni `TourGate` ni `AppTour` están en `TARGETS` de `logger.js` (ver 1.2) — no se auditan ni aparecen en `DDLogger.exportReport()`.

### 4.2 🔍 Buscador
Búsqueda contra la base completa de YGOProDeck.
- **Barra compacta (mobile) y completa:** input de nombre, Buscar, Limpiar, "🎲 Carta Random".
- **Filtros:** palabras clave libres sobre la descripción; filtros avanzados (tipo de carta, atributo, nivel, ATK/DEF, etc.); selector de Arquetipo; selector de Packs y Sets.
- **Resultados:** grid de cartas; clic abre `CardViewer`.
- **Imagen de portada:** columna izquierda del Buscador (y banner de Mi Deck vacío) elegible entre varias opciones (`Buscador.setSidebarImage`).
- **Calidad de imagen:** preferencia de calidad de imagen de carta, guardada en `yugioh_img_quality` (`Buscador.setImgQuality`).
- **CardViewer:**
  - Stats: Nivel → Atributo → Tipo/Clase de monstruo → ATK/DEF.
  - "Carta:" (Effect Monster/Spell/Trap…) y "Arquetipo:" como link clicable que filtra el Buscador.
  - ID, Lanzamiento TCG/OCG, disponibilidad Master Duel/Duel Links, Pack/Set.
  - **Aporte al deck activo:** simula +1 copia y muestra el delta de Consistencia/Potencia/Resiliencia/Internal Score.
  - **Posibles Roles:** roles que se asignarían automáticamente si se agregara al deck activo.
  - **Lore de la carta:** historia del personaje (o del arquetipo si no hay personaje) desde Yugipedia. **Confirmado en esta pasada:** `CardViewer._loadLore(card)` arma el contenido (`_sectionsFromCharacterPage` / `_sectionsFromArchetypePage` / `_wikitextToPlain`), `openLorePanel()` abre un panel con pestañas (`_switchLoreTab(idx)`) — no es un bloque único fijo, puede haber más de una sección/tab según el caso.
  - "🖼 Ver HD" para imagen a tamaño completo.
- **Favoritas:** cartas marcadas, accesible desde el sidebar de Mi Deck.

### 4.3 🗂️ Mi Deck
Gestión del deck activo — 5 sub-tabs + sidebar.

**Sidebar:** Engines | Decks | Staples | Favoritas.

**💾 Botón flotante "Guardar Deck"** *(nuevo, confirmado en esta pasada, no documentado antes)* — `Deck.createSaveFloatingBtn()` inyecta un botón flotante fijo con icono 💾 que llama a `Deck.saveDeck()`. Ocupa el mismo espacio en pantalla que el widget flotante de selección de deck de Estadísticas (ver 4.4), pero con visibilidad inversa: `Deck.updateSaveFloatingBtnVisibility(tabName)` (invocado desde `Navigation.showTab`, ver 8.1) solo lo muestra en la pestaña Mi Deck. Además, `Deck.saveNotes()` permite un guardado rápido de **solo las notas** desde Optimización sin navegar a Decklist ni disparar el guardado completo del deck — no toca `cards` ni pisa `savedAt` salvo que el deck todavía no tuviera ningún guardado previo.

**📥 Import/Export Deck**
- Importar `.ydk`.
- Importar **Lista Oficial en PDF** de Konami (ver flujo 8.10): detecta Main/Extra/Side por encabezados de columna, arma el deck, lista lo no reconocido.
- Guardar deck (nueva versión con diff respecto a la anterior si cambiaron las cartas).
- Exportar `.ydk`, `.txt`, imagen de decklist (`.png`), o **Data completa** (bundle: cartas, Carta As, notas, versiones, Optimización, Complejidad, Matchups y Combos).
- Importar Data completa.

**📋 Decklist**
- Encabezado con nombre editable, chips de conteo por zona (`🃏 Main` / `✨ Extra` / `🔄 Side`), indicador de puntos si la Banlist activa es Genesys.
- **Vista Detallada / Resumida** *(confirmado en esta pasada, ya existía sin documentar)*: toggle `Deck.setDecklistViewMode('detallada'|'resumida')`, persiste en `dd_decklist_view_mode`, default `'resumida'` en un deck nuevo.
  - **Detallada** (`renderRows`) — fila completa por carta: imagen, badges de subtipo/rol, badge de estado en Banlist (`Banlist.getBadgeHTML`), stepper de cantidad, botón mover de zona, botón Rol.
  - **Resumida** (`renderRowsSummary`) — grid compacto tipo "Ver Deck" (imagen + `x${qty}`, sin badges ni acciones), clic abre `CardViewer`; internamente sigue el mismo orden que la vista Detallada. Las cartas del Side Deck se muestran con la imagen desaturada (`.deck-img-desaturated`) para diferenciarlas de un vistazo.
  - Aplica de forma independiente a las 3 zonas (Main/Extra/Side comparten el mismo modo elegido).
- **Side Deck — reglas confirmadas en esta pasada:**
  - **Orden:** primero las cartas de origen Main Deck (mismo criterio de orden que el Main), después las de origen Extra Deck al final (Fusión→Sincronía→Xyz→Link) — `Deck.getSideDeckCardType()`.
  - **Tope de 15:** al mandar una carta del Extra Deck al Side (`Deck.moveExtraToSide`) se valida contra el límite real de 15 cartas de Side Deck y se bloquea con alert si se excede. **Este tope NO se valida** cuando una carta de Main pasa a Side por el botón normal (`Deck.toggleLocation`) — queda como posible inconsistencia a resolver si se quiere el mismo tope para ambos caminos.
  - Una carta que volvió de Side y originalmente venía del Extra Deck, al tocar "volver" regresa a Extra (no a Main) — respeta su origen real.
- Filas de carta con acciones rápidas (ver, quitar, marcar Carta As).
- **🧭 Tu Experiencia con el Deck** (6 sub-tabs): Perfil (dificultad 1-5★, estrategia, variante, non-engine slots) · Manos Muertas (X muertas / Y jugadas) · Composición (top atributos/tipos, automático) · Cartas Destacadas (selección manual por categoría) · Sets (packs detectados, marcables) · Rendimiento (gráfico de araña Consistencia/Potencia/Resiliencia/Techo de Poder G1+G2 + winrate de Duelista). **Bug de la v6.0 resuelto:** `Deck.switchExperienciaTab` ya solo tiene una definición y sí incluye `'cartas'` y `'sets'` en la lista de panes — el clic en esos dos sub-tabs funciona correctamente.
- **Historial de Versiones:** versiones guardadas con diff, comentario editable, score promedio de las sesiones de Optimización de ese rango.
- **📌 Cartas Clave y Amenazas:** ranking agregado de cartas marcadas clave/amenaza en rondas de Optimización.
- **Notas del Deck**, **⚔️ Historial de Enfrentamientos** (Matchups embebido), **🎖️ Nivel como Piloto** (Duelista embebido).

**🔨 Construcción**
Asignación/revisión de roles por carta (auto o manual), composición del deck, tarjeta de resultado (solo lectura) de Complejidad del Deck.

**🎯 Optimización**
Registro de rondas de duelo por matchup (ver 7.1–7.3 y 8.6 para detalle de campos y flujo). Sesiones etiquetables con badges de calidad. Incluye Complejidad del Deck, Duelista y Matchups embebidos.

**🧬 Línea de Combos** *(confirmado por primera vez a nivel de inventario de métodos — antes solo prosa funcional)*
Mapeo de combos por zonas (mano/campo/cementerio/baneadas): starters y Boss Monster auto-detectados (override manual) · choke points con nivel de riesgo/frecuencia · restricciones por severidad y dependencias entre pasos · **"copia de"** (marca una copia de carta como uso alternativo de otra dentro de la línea) · **interacciones** automáticas + manuales entre cartas · endboard con función por carta · **objetivo del combo** (editable) · ramificaciones (combos hijos por choke point/variante) · Power del combo + consistencia inicial (hipergeométrica) · **modo de ejecución paso a paso** — robar carta, mover entre zonas, log de jugadas coloreado por zona (`startCombo`/`drawCard`/`moveCard`/`finishCombo`/`reopenCombo`) para simular la línea jugada a jugada, no solo mapearla en abstracto · export/import como `.txt` **en dos variantes** (`exportComboTXT` simple / `exportComboFull` completo).

### 4.4 📊 Estadísticas
- **🔄 Actualizar Data:** recalcula en cadena todos los scores del meta (enriquece cartas faltantes, Internal Score por deck, Cross-Score N×N) y Poder de Cartas, con barra de progreso por fases (ver flujo 8.5).
- **🏆 Top Tier:** ranking de decks (propios + meta) en Tier 1/2/3/Fun por score promedio; filtrable por Powercreep, Consistencia, Potencia o Resiliencia.
- **🗂️ Carpetas del Meta:** organiza decks del meta por carpeta; import `.ydk`; export/import de carpetas como "Chips".
- **Decks del Meta:** listado filtrado por carpeta, ver/cargar/eliminar.
- **Poder de Cartas del Meta:** score de poder por carta según presencia y desempeño en decks del meta.
- **Análisis de Deck:** Internal Score, External Score, Going First/Going Second (G1/G2 + perfil), vulnerabilidades G1/G2, especializaciones, bloques de Duelista.
- **📥 Exportar** *(nuevo, confirmado en esta pasada, no mencionado en versiones previas del reporte)*: `exportDeckReport()` (reporte del deck activo, descarga `.txt`), `exportMetaPowerRanking()` (ranking de Poder de Cartas del meta) y `exportMetaFrequency()` (frecuencia de cartas en el meta) — **corregido en v9.0:** estos dos últimos descargan `.csv`, no `.txt` (`_downloadTxt()` es un nombre de método genérico que descarga cualquier extensión pasada por parámetro, no solo `.txt`).
- **Widget flotante de deck** *(nuevo, confirmado)*: selector de deck activo persistente entre pestañas (`createDeckFloatingWidget`, lista expandible/colapsable con `expandDeckList`/`collapseDeckList`), visible u oculto según la pestaña actual vía `updateFloatingWidgetVisibility` (ya referenciado mecánicamente en el flujo 8.1 desde la v6.0, pero nunca descrito como feature hasta ahora).

### 4.5 🎮 Simuladores
- **🎲 Mulligan (Hipergeometría):** probabilidad de abrir cierta combinación en mano inicial (distribución hipergeométrica sobre el deck actual). **Ampliado en v9.0 — confirmado 3 modos** (`Hipergeometria.switchTab()`), no documentados antes: **Cálculo Estándar** (hipergeométrica manual, entrada libre de combinación buscada), **Cálculo con Mis Decks** (usa directamente un deck guardado en vez de armar la combinación a mano) y **Prueba Mulligan** (simulación visual de apertura de mano, no solo el cálculo de probabilidad).
- **📊 Winrate:** registro manual ganadas/perdidas, general o por deck.
- **🏆 Torneo:** simulador Swiss — rondas, emparejamientos, tabla de posiciones.
- **⚔️ Duelo en Vivo:** asistente para un duelo real en curso — Cronómetro Estándar y Cronómetro Master Duel (temporizador por turno).
- **🛡️ Counters:** arma un pool (deck activo, Engine, Staples, Favoritas o búsqueda manual), marca cartas objetivo del rival, busca sus counters conocidos; cada counter puede marcarse como **⭐ Perfect Counter** (`togglePerfect`, no documentado antes); el resultado puede convertirse en Engine.
- **🥊 Gauntlet:** testing de deck/engine/pool manual contra "Pruebas" del usuario (Éxitos/Fallos a mano o en Zona de Práctica), con chips de decks del meta como oponentes (excluyendo cartas puntuales), Ranking global (**exportable/importable como `.txt`**, no documentado antes) y plantillas reutilizables.
- **🧪 Experimentación:** lienzo libre — cartas buscadas, `.ydk` o de un deck guardado — para plantear líneas de juego visualmente, con zoom.
- **🎴 Zona de Práctica:** simulación de campo por zonas para practicar jugadas. **La v6.0 solo describía "guarda/recupera estados por deck" — confirmado en esta pasada que es bastante más:**
  - **Resolución de cadena** con numeración de chain links y botón de resolución (simula prioridad/Spell Speed).
  - **Información oculta** por zona — ocultar mano/campo completos o voltear todo boca abajo, pensado para practicar contra otra persona sin revelar información.
  - **Visor de mazo/cementerio/baneadas** que permite buscar, voltear, reordenar y mover cartas directo desde ahí (`openDeckViewer` + `_dvSendCard`/`_dvFlip`/`_dvMoveInZone`/`_dvRemoveCard`).
  - **Log de duelo** con fases y jugadas, exportable como `.txt` (`downloadLog`).
  - **Navegador de estados guardados** con preview de campo antes de restaurar, borrado individual o masivo (más rico que un simple guardar/cargar).
  - **Exporta el estado del campo como imagen `.png`** (`_downloadStatePng`).
  - Guarda/recupera estados por deck (esto sí ya estaba documentado en la v6.0).

### 4.6 🎓 Formación
- **📓 Apuntes:** notas propias — guardar, abrir, borrar, export/import `.txt`.
- **📚 Temas:** biblioteca educativa competitiva (vocabulario legal, fases del duelo, tipos de carta, estructura de efectos y arquetipos, mentalidad de juego, cadenas y prioridad, staples, anatomía de deck competitivo, debilidades de deck, elegir/construir/optimizar un deck, equilibrio de deck, rulings de invocación/batalla, timing if/when, lectura de campo rival, gestión de LP y recursos, diferencias entre formatos, side deck, tiers del meta, qué es Yu-Gi-Oh y cómo practicar para un evento, etc.)
- **📜 Historia del Meta:** eras TCG/OCG, campeonatos mundiales (Worlds), campeones de Master Duel.
- **🧪 Test:** quizzes teóricos (opción múltiple) y prácticos (armar campo objetivo contra campo rival dado); herramienta de autoría propia, export/import. **Confirmado en esta pasada:** los tests custom se guardan en `yugioh_custom_tests` vía el objeto global `TestDuelo` (`getAll/get/save/remove/exportTest/importFromText`), que no depende de ningún otro módulo. También se gestionan desde Config → 🧪 Test de Duelo (ver 4.7).
- **🧭 Tu Estilo:** quiz de ejes de preferencia → recomienda arquetipo afín, con link al Buscador. Resultado guardado en `yugioh_estilo_juego` (confirmado en esta pasada, clave no listada antes).
- **🎭 Tu Personaje:** quiz similar → recomienda personaje del anime/manga afín. Resultado guardado en `yugioh_personaje_resultado` (confirmado en esta pasada, clave no listada antes).
- **🃏 Primeros Decks:** decklists `.ydk` por nivel (Básico/Intermedio/Competitivo), un clic las carga en Mi Deck. **Discrepancia de la v6.0 resuelta:** viven en `decklists.js` (`DecklistsData`), y ese `<script>` sí está cargado en `index.html`. **Actualizado en v9.0:** ya no son 10 decks — `DecklistsData.decks` trae **204** entradas `{name, filename, level, ydk}`. `getByName(name)` busca case-insensitive; `getNames()` devuelve solo nombres; `getGroupedByLevel()` agrupa por `level` y ordena alfabéticamente (`localeCompare('es')`) dentro de cada grupo, con `'Basico'` como fallback si `level` no coincide con los 3 valores esperados.
- **🎮 Juegos:** juegos y variantes relacionadas a Yu-Gi-Oh!, con plataforma.
- **🔗 Fuentes:** fuentes externas del meta embebidas en iframe (fallback a link externo si el sitio bloquea el embed).
- **🎓 Maestros:** perfiles de jugadores destacados, video embebido (YouTube) cuando aplica.

### 4.7 ⚙️ Config
```
Roles y Palabras Asociadas    (panel colapsable por rol — ver 6.2)
Counters                      (pares mecánica/counter, 🗑️ por par)
Staples                       (cartas esenciales del formato)
Nomenclatura de Efectos       (panel colapsable por categoría — ver 6.2)
Pilares del Internal Score    (roles por pilar + RPS con opción "Ninguno")
Scoring Avanzado (G1/G2)      (ver sección 5.6 — 7 métodos save)
Rendimientos Decrecientes
Atajos Rápidos
Banlist del Formato           ("＋ Agregar carta" → mini-buscador CardViewer, ver 8.11;
                                puede haber varios formatos ACTIVOS a la vez
                                (getActiveFormats/toggleActiveFormat); se pueden crear
                                y borrar formatos CUSTOM propios
                                (createCustomFormat/deleteCustomFormat); cada formato
                                tiene un toggle "invertido" (toggleInverted) — ninguna
                                de estas 3 mecánicas estaba documentada antes)
Música
Intro de Pestañas             (nuevo, confirmado: activa/desactiva el overlay de
                                bienvenida por pestaña — TabIntro, ver core.js y 3.
                                Sección propia dentro de Config, NO vive dentro de
                                "Contenido de la App" como asumía la v6.0)
Maestros del Duelo            (nuevo, confirmado: gestión de los perfiles de Maestros
                                que se muestran en Formación/Meta)
Fuentes Externas del Meta     (nuevo, confirmado: gestión de los links embebidos que
                                se muestran en Formación → Fuentes)
Juegos Alternativos de Yu-Gi-Oh! (nuevo, confirmado: gestión del contenido de
                                Formación → Juegos)
Temas de Formación            (nuevo, confirmado: gestión/activación de los temas de
                                Formación → Temas)
Test de Duelo                 (nuevo, confirmado: alta/edición/export/import de Tests
                                custom — backing store es TestDuelo/yugioh_custom_tests,
                                ver 4.6)
Contenido de la App           (visibilidad granular por perfil, tema de fondo —
                                renderizado aparte por ContentManager.renderConfigSection()
                                al tope de Config.render(), NO es un `.config-section`
                                colapsable como el resto de esta lista)
Exportar / Importar Data      (importar pide confirmación; exportar detalla contenido)
Restaurar Configuración       (re-ejecuta DefaultData.init())
Zona de Borrado               (16 categorías selectivas — ver sección 3, confirmado
                                contra el HTML real en esta pasada)
Acciones adicionales          (nuevo, confirmado: "📧 Reportar Error" y "📋 Generar
                                Reporte" — ver flujo 8.13)
```

### 4.8 ❓ Panel de Ayuda / FAQ (`HelpPanel`) *(nuevo en el reporte v9.0 — el objeto ya figuraba en la tabla de módulos de `formacion.js` desde v7.0 pero nunca se había descrito su funcionalidad)*
Panel global flotante, disponible en **todas** las pestañas — no es contenido de una sección de Config, vive suelto en `formacion.js` y se auto-inicializa en `DOMContentLoaded`.
- **Botón flotante `?`** (`help-float-btn`) fijo en pantalla, abre/cierra el panel (`HelpPanel.toggle()`).
- **Tab "Ayuda":** contenido contextual según la pestaña activa (`HelpPanel.getActiveTab()` lee `Navigation.currentTab`) — texto explicativo de cada sección de esa pestaña, tomado de `HelpPanel.tabContent[tab]` (una entrada por cada una de las 7 pestañas + `default`). Se actualiza solo si el panel está abierto y el usuario cambia de pestaña con el panel visible, ya que `showTab()` vuelve a leer la pestaña activa cada vez.
- **Tab "FAQ":** lista fija de preguntas frecuentes (`HelpPanel.faqContent[]`, acordeón colapsable por pregunta vía `toggleFAQ(i)`) centrada en explicar el sistema de scoring y las mecánicas menos obvias de la app: qué es el Internal Score, el G1/G2 Score, el External Score (deck activo y meta), el **Counter-Deck Score** (ver 5.3bis), el peso de roles vs Poder Base, cómo exportar/importar toda la data, qué es "Nivel como Piloto del Deck", qué hace "Restaurar Configuración", Especialidades y Counters, Línea de Combos, Gauntlet, copias de una carta repartidas entre Main y Side, Banlist con varios formatos activos, diferencia entre Vista Detallada/Resumida, el tope de 15 cartas del Side Deck, y Tu Estilo/Tu Personaje.
- No depende de ningún otro módulo para renderizar — solo lee `Navigation.currentTab` si existe, con fallback a buscar `.nav-tab.active`/`.tab-btn.active`/`[data-tab].active` en el DOM.

---

## 5. SISTEMA DE SCORING — DETALLE

### 5.1 Internal Score
`Stats.calculateInternalScore(cards)`. Loop de roles con rendimientos decrecientes por pilar. Devuelve `{internalScore, consistency, power, resilience, totalCards, mainCards, penalty, g1Score, g2Score}`.

### 5.2 G1/G2 Score

Calculado dentro del mismo loop de `calculateInternalScore()` (`data.js`, `for` sobre `cards`, dominio `loc === 'main'`):

```
Por cada carta (loc='main'):
  1. segments = NomenclatureAnalyzer.segmentDescription(rawDesc)
     → segmenta la descripción en categorías de nomenclatura
       (condicionActivacion, costoActivacion, restriccion, efectoGenerico...)

  2. layerMult = producto de L1 x L2 x L4 x L5
     por cada capa (layers = ConfigManager.getScoringLayers()):
       texto = segments[layer.nomenclatureCategory]
       busca la primera entry cuyo keyword aparezca en el texto
       multiplica por su .multiplier (si no matchea ninguna: x1.0)

  3. pool = qty de la carta
     + copias de cualquier Searcher en el main que mencione su nombre/arquetipo
       en su propia descripción (pool indirecto vía buscadores)

  4. reliability = ConfigManager.getReliability(pool)
     tabla por copias: 1→0.40, 2→0.65, 3→0.85, 4→0.90, 5→0.95 (config., default)

  5. por cada rol de la carta (item.roles):
       basePower = ConfigManager.getRoleBasePower(rol)      // L3, default 3
       cardScore = basePower x layerMult x reliability
       context   = ConfigManager.getRoleG1G2(rol)           // 'g1'|'g2'|'neutral'
       g1 → g1Score += cardScore
       g2 → g2Score += cardScore
       'neutral' → mitad y mitad (g1Score += cardScore*0.5, g2Score += cardScore*0.5)
```

```
CardScore = BasePower(L3) x L1_mult x L2_mult x L4_mult x L5_mult x Reliability

L1 — Velocidad:   keywords en 'condicionActivacion'
L2 — Coste:       keywords en 'costoActivacion'
L4 — Restricción: keywords en 'restriccion'
L5 — Alcance:     keywords en 'efectoGenerico'
```

**Clasificación G1/G2 por rol (default, `g1g2Roles` en `data.js`):** roles de apertura de mano/setup (`Starter`, `Extender`, `Booster`, `Boss Monster`, `Tower`, `Token Summoner`, `Ignition`, familia `Stun-*`) → **g1**. Roles de respuesta/interacción (`Handtrap`, `Boardbreaker`, `Disruptor`, `Removal`, `Negate-activation`, `Negate-effect`, `Negater`, `Handloop`, `Speed-4`, `Anti-damage`) → **g2**. El resto (`Searcher*`, `Recycler`, `Draw-engine`, `Protector`, etc.) → **neutral**.

**Renderizado** — `Estadisticas.renderDeckAnalysis()`: bloque Going First/Going Second con `g1Score`/`g2Score` (`.analysis-g1g2-row/-item/-label/-val`); `g1g2Profile` (diferencia >1.3x → "dependiente del dado" o "reactivo"; si no, "equilibrado", en `.analysis-g1g2-profile`); si `hasPowerData && hasSpecData`, también `g1Vulnerability`/`g2Vulnerability` (0-10) de `calculateExternalScore()`.

**Decisión de diseño (no bug):** `g1Vulnerability`/`g2Vulnerability` clasifican **specialties/mecánicas** reutilizando `getRoleG1G2()` — pensada para **roles de carta**, no para nombres de mecánica. Si una specialty no coincide textualmente con una key de `g1g2Roles`, cae en `'neutral'` y no suma a ninguna vulnerabilidad. Confirmar con el usuario si se quiere una tabla G1/G2 separada para specialties antes de tocar esta área.

### 5.3 External Score (deck activo)
`Stats.calculateExternalScore(cards, powerScoreCache, metaDecks)` — cruza specialties del deck con `powerScoreCache`, RPS modifier (ignorando `loser='ninguno'`). Devuelve `{externalScore, g1Vulnerability, g2Vulnerability, deckSpecs, threatCards, counterDecks, missingStaples, hasPowerData, hasSpecData, baseline, totalThreat, ...}`. `counterDecks` (hasta 5) alimenta el bloque "Mayor amenaza" del Análisis de Deck en Estadísticas.

### 5.3bis Counter-Deck Score *(nuevo, confirmado en esta pasada — no documentado en ninguna versión previa del reporte)*
`Stats.calculateCounterDeckScore(cards, powerData)` — mide la capacidad **Anti-META** del deck activo: qué tan preparado está para interrumpir las estrategias del meta cargado, a diferencia del External Score (que mide qué tan expuesto está el deck a ser interrumpido).

```
Por cada carta en Main/Extra (item.location):
  si tiene rol 'brick' → suma a brickCount, no aporta counter, sigue a la siguiente
  si está en powerScoreCache y cached.isCounter && cached.counterBonus > 0:
      contrib = cached.counterBonus × qty  → suma a rawCounter
  si NO hay powerScoreCache pero SpecialtyAnalyzer detecta counters en la carta
  (item.data): fallback binario, contrib = 5 × qty (marcado estimated:true)

brickRatio   = brickCount / totalCards
brickPenalty = round(rawCounter × brickRatio × 0.6)
finalScore   = max(0, rawCounter − brickPenalty)
```
Clasificación por `finalScore`: 0 → "Sin capacidad Anti-META" · 1-30 → "Anti-META Bajo" · 31-70 → "Anti-META Medio" · 71-120 → "Anti-META Alto" · 121+ → "⚡ Anti-META Élite". Devuelve `{finalScore, rawCounter, brickPenalty, brickCount, counterCards, totalCards, breakdown[], level, levelColor, hasPowerData}`. Consumido por `Estadisticas.renderDeckAnalysis()` y por `exportDeckReport()`. Depende de los mismos pares mecánica/counter configurados en Config → Counters que alimentan el Power Score del meta y el External Score (5.3).

### 5.4 External Score (meta) = Cross-Score N×N
`calculateCrossExternalScores()`. Para cada deck A: amenazas de counters de B ponderadas por `internalScore` de B y RPS. `crossExternalScore = (1 - threat/baseline) × 10`.

### 5.5 RPS
Reglas `[ganador, perdedor]`. `perdedor='ninguno'` → ese pilar no tiene counter. Modificadores: ×1.25 ventaja / ×0.75 desventaja / ×1.0 neutral.

### 5.6 Scoring Avanzado — métodos save en Config
Guardan correctamente en `yugioh_config` y son consumidos en tiempo real por `calculateInternalScore()` (ver 5.2).

| Función | Qué guarda |
|---------|-----------|
| `saveG1G2Role(rol, valor)` | Clasificación G1/G2/neutral del rol |
| `saveRoleBasePower(rol, val)` | Poder Base L3 (0–10) |
| `saveReliabilityEntry(copias, val)` | Probabilidad por n copias (0–1) |
| `saveLayerEntryKeywords(lKey, idx, raw)` | Keywords de una entrada de capa |
| `saveLayerEntryMultiplier(lKey, idx, val)` | Multiplicador de una entrada |
| `addLayerEntry(lKey)` | Añade entrada vacía a la capa |
| `removeLayerEntry(lKey, idx)` | Elimina entrada por índice |

---

## 6. SISTEMA DE ROLES Y NOMENCLATURA

### 6.1 Detección de roles
```
autoAssignRoles(card) → detectPossibleRoles(card)
  por cada rol en roleConditions:
    keywords coinciden en desc
    AND conditionals (si hay) también coinciden
    AND notContains (si hay) NO coinciden
    → rol asignado

roleConditions[roleName]: { keywords:[], conditionals:[], notContains:[] }
```

### 6.2 Pattern de renombrado con input oculto (roles y nomenclatura)
El badge de nombre es siempre el elemento visible; el input se oculta con `display:none`.

| Elemento | Roles | Nomenclatura |
|---------|-------|-------------|
| Span visible | `id="rpn-${roleName}"` | `id="npn-${cat.id}"` |
| Input oculto | `id="rni-${roleName}"` | `id="nni-${cat.id}"` |
| Botón lápiz | `focusRenameInput(roleName)` | `focusNomRenameInput(catId)` |
| onblur | `renameRole(el)` | `renameNomCategory(catId, el)` |
| Clonar | `duplicateRole(roleName)` → `ConfigManager.duplicateRole` | `duplicateNomCategory(catId)` → `ConfigManager.duplicateNomenclatureCategory` |
| Borrar | `deleteRole(roleName)` | `deleteNomCategory(catId)` |

`duplicateNomenclatureCategory(catId)` — deep-copy de la categoría, nuevo ID `custom_` + timestamp, nombre + " (copia)", insertada inmediatamente después de la original. Devuelve el nuevo ID para hacer scroll.

---

## 7. COMPLEJIDAD DEL DECK, DUELISTA Y BOARDBREAKER

### 7.1 Complejidad del Deck (clasificador de dificultad de uso/aprendizaje)

Quiz de 8 preguntas fijas (`Deck.CXD_PREGUNTAS`) repartidas en dos grupos de 4, cada respuesta vale 1-3 puntos:
```
grupo 'aprender' (curva de entrada):   flujo del deck, ejecución de combos,
                                        margen de error para un novato, lectura de cartas
grupo 'dominar'  (techo de habilidad): recursos a controlar, densidad de decisiones,
                                        conocimiento del meta requerido, adaptación
                                        ante interrupción rival
```
`Deck.calcularComplejidad()` suma cada grupo (0-12 c/u, total 0-24) y guarda en `complejidad_${Deck.name}` (`{answers, aprender, dominar, total, evaluatedAt}`).

`Deck._complejidadNivel(total)` clasifica en 5 tramos (curva de aprendizaje y techo de habilidad son ejes independientes — un deck puede ser fácil de aprender pero difícil de pilotar al máximo nivel, o viceversa):
```
0-11   🟢 Ideal para empezar
12-15  🟢 Principiante
16-18  🟡 Intermedio
19-21  🟠 Avanzado
22-24  🔴 Experto
```
Se renderiza en dos lugares con `Deck.renderComplejidadResultCard()`: Mi Deck → Optimización → bloque completo (con formulario), y Mi Deck → Construcción → `#construccion-complejidad-box` (solo resultado, sin form). No se recalcula solo — el jugador debe re-evaluar manualmente si cambia el deck.

### 7.2 Duelista / "Nivel como Piloto del Deck"

`Duelista.getDeckStats(deckName)` lee **exclusivamente** las rondas de `Deck.getOptimizacion(deckName)` (pestaña Optimización) del **deck actualmente cargado** — no tiene relación con `Matchups` (winrate manual por rival) ni con otros decks guardados.

```
Duelista.LEVELS (por rondas totales del deck activo):
  0 Novato · 5 Aprendiz · 15 Piloto · 30 Rival · 50 Competidor ·
  100 Semi-Pro · 200 Pro · 400 Élite · 800 Legendario
```
`Duelista.renderSection()` pinta nivel actual, barra de progreso al siguiente nivel, winrate general / going 1st / going 2nd calculado solo con esas rondas.

`Duelista.refreshSection()` re-renderiza **ambas** instancias visibles: `#duelista-content` (Estadísticas) y `#duelista-content-opt` (Mi Deck → Optimización, sobre Complejidad del Deck).

**Integración con Matchups:** cada fila de "⚔️ Historial de Enfrentamientos" es clicable — `Matchups.selectDeckFilter(nombreRival)` filtra "📊 Historial de Sesiones" (rondas) para mostrar solo las de ese rival, re-renderizando `#mideck-optimizacion-pane`. `clearDeckFilter()` quita el filtro. Es puramente visual/UI — no afecta el cálculo de Duelista (que siempre usa TODAS las rondas del deck, filtradas o no en pantalla).

### 7.3 Campo "Boardbreaker en mano" y "Bricks/Tech en mano"

En el formulario de Rondas de Duelo (Mi Deck → Optimización → 🃏 Mano Inicial), entre Handtraps y Bricks: select `opt-r-boardbreaker` (0/1/2/3+) para cartas rompe-campo (Raigeki, Dark Ruler No More, Evenly Matched, etc.) en mano inicial — relevante jugando de segundo, donde el plan de juego depende de romper el campo rival.

```
Guardado:    round.boardbreakers = n('opt-r-boardbreaker')  (addOptimizacionRound)
Métrica:     m.avgBoardbreaker — promedio de boardbreakers por ronda (calcOptMetrics)
Sugerencia:  getOptDiagnostics() — si avgBoardbreaker < 0.5 y hay ≥3 rondas jugando de
             segundo (m.rSecond.length >= 3): "⚠ Pocos Boardbreakers en mano jugando de
             segundo. Considera sumar cartas que rompan el campo rival (...)"
Visible en:  chip de resumen de sesión (🧨 øX.X BB) y detalle de cada ronda (🧨X junto
             al tag de Handtraps).
```
Campo independiente del cálculo de Brick Rate — no resta ni suma a `bricks`/`br`, solo alimenta su propio promedio y sugerencia.

**"Bricks en mano" → "Bricks/Tech en mano":** cambio de rótulo visible únicamente. El id (`opt-r-bricks`), el campo guardado (`round.bricks`) y todo el cálculo (Brick Rate, diagnósticos, export a Matchups) siguen tratando el valor exactamente igual — como negativo/brick. No hay distinción real a nivel de datos entre "brick puro" y "tech situacional", solo de nomenclatura visible.

---

## 8. FLUJOS CRÍTICOS

> Confirmados en la última auditoría línea-por-línea. Válidos salvo que el módulo en
> cuestión haya crecido después (ver aviso al inicio del reporte) — en ese caso se
> marca explícitamente. La auditoría estructural v7.0 confirma que los objetos y
> métodos que participan en estos flujos siguen existiendo con esos nombres, pero
> **no** re-verificó el cuerpo interno de cada función salvo donde se indica lo
> contrario (8.9 y 8.13 sí quedaron confirmados a fondo en esta pasada).

### 8.1 Cambio de pestaña
```
switchTab(name) → Navigation.showTab(name)
  → oculta todos .tab-content, activa #name-content
  → Estadisticas.updateFloatingWidgetVisibility(tabName)
  → Deck.updateSaveFloatingBtnVisibility(tabName)      ← nuevo, confirmado v9.0 (ver 4.3)
  → si simuladores:       Torneo.init()
  → si mideck:             Engines.init()
  → si saliendo de simuladores: ZonaPractica._cleanupFloatBtns()
  → si meta:               Meta.init()
  → si formacion:          Formacion.init()
  → si lobby:              Lobby.init()                ← nuevo, confirmado v9.0
  → window.scrollTo({ top:0, behavior:'smooth' })
  → si typeof updateButtons === 'function': updateButtons()
  → window.dispatchEvent(new Event('scroll'))
  → ContentManager.applyAll()
  → TabIntro.maybeShow(tabName)                        ← punto de enganche RESUELTO en v9.0
```
**Resuelto en v9.0:** `TabIntro.maybeShow(tabName)` se dispara al final de `Navigation.showTab()`, en `core.js`, como el último paso — después de `ContentManager.applyAll()`. La v7.0-v8.0 no la habían encontrado ahí porque quedó agregada en una edición posterior de `core.js` (junto con `Deck.updateSaveFloatingBtnVisibility` y `Lobby.init`, ambos también nuevos en esta llamada). El tema de fondo `dd_bg_theme` sigue aplicándose vía `ContentManager.applyAll()`, no vía `TabIntro`.

### 8.2 Agregar carta al deck (desde Buscador)
```
Buscador.addCard(index)
  → Deck.syncFromViewer(card.id, card, currentQty + 1)
  → si carta nueva: autoAssignRoles() + SpecialtyAnalyzer + NomenclatureAnalyzer
  → Deck.render()  → recalcula Internal Score en tiempo real
    (Stats.calculateInternalScore — el mismo cálculo actualiza g1Score/g2Score, ver 5.2)
  → External Score se recalcula si powerScoreCache existe
```

### 8.3 Importar .ydk a Mi Deck
```
Deck.importYDK()
  → input[file] .ydk → parseYDK(content, filename)
  → overlay de carga "Importando deck... N cartas"
  → fetch API YGOProDeck (batch por IDs únicos)
  → construye newCards con autoAssignRoles()
  → Deck.cards = newCards; Deck.render(); Deck.onDeckLoaded()
  → elimina overlay → alert "Deck importado"
```

### 8.4 Cálculo de scores — ciclo completo
```
Stats.calculateInternalScore(Deck.cards)
  → loop de roles → rendimientos decrecientes por pilar (consistency/power/resilience)
  → EN EL MISMO LOOP, por carta: calcula G1/G2 Score (fórmula completa en 5.2)
  → devuelve { internalScore, consistency, power, resilience,
               totalCards, mainCards, penalty, g1Score, g2Score }

Stats.calculateExternalScore(Deck.cards, powerScoreCache, metaDecks)
  → cruza specialties del deck con powerScoreCache
  → RPS modifier (ignorando loser='ninguno')
  → devuelve { externalScore, g1Vulnerability, g2Vulnerability, ... }
```

### 8.5 Actualizar Scores del Meta — flujo automático completo (async)
```
Estadisticas.recalculateAllMetaDeckScores()  ← async
  → lista todos los decks con secciones (sin importar si fueron abiertos antes)
  → crea overlay #meta-update-overlay con spinner + barra de progreso + nombre del deck actual

  FASE 1 (0–60%) Enriquecimiento automático:
    por cada deck en la lista:
      → IDs no presentes en metaCardLibrary → fetch API en batches de 10
      → autoAssignRoles() + SpecialtyAnalyzer para cada carta nueva
    → _saveMetaCardLibrary()

  FASE 2 (60–92%) Cálculo de scores:
    por cada deck:
      → _computeAndSaveMetaDeckScore(folder, filename)
      → Internal Score + pilares con fakeCards desde la librería

  FASE 3 (92–100%) Cross-Score N×N:
    → calculateCrossExternalScores()
    → metaDeckScores[key].externalScore = crossExternalScore
    → guarda yugioh_cross_scores + yugioh_meta_deck_scores

  → "¡Listo! N decks actualizados" 700ms → overlay desaparece → render()
```
Un solo clic procesa todos los decks — no hace falta abrirlos manualmente.

### 8.6 Selección de perfil
```
Welcome.enter(tabName, levelKey)
  → ContentManager.applyProfile(levelKey)  → 'dd_player_profile'
  → ConfigManager.savePlayerLevel(levelKey) → 'yugioh_player_level'
  → MusicPlayer.setTrack(cfg.tracks[levelKey])
  → Welcome.dismiss() → 'dd_welcome_dismissed' = 'true'
  → Navigation.showTab(tabName)
```

### 8.7 Borrar Todo / Restaurar Config
```
Config.borrarTodo()
  → localStorage.clear() + _resetModulesInMemory() + Welcome.init()

Config.resetToDefault()  ← parcheado por DefaultData
  → localStorage.clear() + await DefaultData.init()
```

### 8.8 DefaultData.init() — inyección única por tipo
```
window.load → DefaultData.init()
  → _initNotes / _initSpecialties / _initGames / _initMasters
  → _initDecks / _initStaples / _initEngines / _initGenesys  (fetch API)
  → _patchConfig() → sobreescribe Config.resetToDefault
```

### 8.9 Abrir CardViewer (panel "Ver" de carta)
```
CardViewer.open(card)
  → pinta overlay síncrono con lo que ya trae `card`:
      Carta: card.type ("Carta:")
      Nivel → Atributo → Tipo (card.race; "—" si no es monstruo) → ATK → DEF
      Arquetipo: card.archetype como link, o "—" sin link si no tiene
      ID: card.id (en --gold-color)
  → fetch cardinfo.php?id=${card.id}&misc=yes (un único fetch, async)
      → card_sets  → rellena "📦 Pack / Set"
      → misc_info[0].tcg_date / ocg_date → "Lanzamiento TCG:" / "Lanzamiento OCG:"
        (fmtDate → toLocaleDateString('es-ES'); "No disponible" si falta)
      → misc_info[0].formats → "Disponible en:" (Master Duel / Duel Links).
        Fila oculta por completo si no hay match en ninguno de los dos.
  → click en Arquetipo → CardViewer.openArchetypeInBuscador(name)
      → cierra overlay → Navigation.showTab('buscador') → Buscador.clear()
      → Buscador._loadArchetypes() → advancedFilters.archetype = name → autoSearch()
  → si el deck activo tiene cartas: renderCardContribution(card)
      → detectPossibleRoles(card) — como autoAssignRoles pero sin persistir,
        solo para listar "🎯 Posibles Roles"
      → calculateCardContribution(card, roles) — clona Deck.cards, simula +1
        copia (o la inserta si no está) y compara calculateInternalScore() antes/después
      → pinta "📊 Aporte al deck activo": barras de Consistencia/Potencia/
        Resiliencia + delta de Internal Score. Se oculta si el deck está vacío o si
        ContentManager.isVisible('cv-roles'|'cv-contribution') lo desactiva
  → si el deck activo NO tiene cartas o el usuario abre el botón de Lore:
      CardViewer._loadLore(card) — fetch a Yugipedia (wikitext de la página del
        personaje o del arquetipo), fallback a proxy CORS (api.allorigins.win) si
        falla el fetch directo → _sectionsFromCharacterPage / _sectionsFromArchetypePage
        parsean el wikitext → _wikitextToPlain limpia el markup → openLorePanel()
        pinta el panel con pestañas (_switchLoreTab), _renderLoreButton controla el
        botón que dispara todo esto. **Confirmado a fondo en esta pasada** —
        reemplaza la nota de la v6.0 que decía "no confirmado".
```
**Importante:** `tcg_date`, `ocg_date` y `formats` SOLO existen dentro de `misc_info[0]` cuando se pasa `misc=yes` — nunca en la raíz del objeto de la carta.

**Funciones sin usar (no tocar salvo que se pida reactivarlas):** `CardViewer.generateCardDecklistHTML2Canvas()`, `generateCardDecklistCanvas()` existen (exportar la carta como imagen tipo decklist) pero no están enganchadas a ningún botón — "🖼 Ver HD" solo abre la imagen a tamaño completo. `generateCardDecklistFallback()` sigue presente también, mismo estado.

### 8.10 Importar Deck desde Lista Oficial (.pdf)
```
Deck.importPDF()
  → input[file] .pdf → Deck.parsePDFDeckList(file)
  → overlay "⏳ Leyendo Lista Oficial (.pdf)..."
  → _pdfExtractEntries(file) — usa pdfjsLib (pdf.min.js) para leer texto por página:
      → agrupa items por línea (tolerancia Y) y por clusters (gap X) → detecta
        encabezados de columna (Monster/Spell/Trap Cards, Extra Deck, Side Deck) y
        arma rangos de X (columnDefs) para asignar cada línea a main/extra/side
      → descarta líneas de ruido (encabezados de juez, paginación, códigos de barra)
      → por cada línea de carta genera candidatos {qty, name} (cantidad al inicio o
        al final del texto)
      → si no se detectó NINGÚN encabezado → Error "PDF no reconocido"
  → overlay "⏳ Buscando cartas en la base de datos..."
  → _pdfFetchFullCardDB() — fetch sin filtros a cardinfo.php (base completa, sin
    caché) para matchear cualquier nombre
  → _pdfMatchEntries() — matchea cada candidato por nombre exacto y, si falla, por
    nombre normalizado (espacios colapsados); acumula cantidades por id+zona; lo que
    no matchea queda en unknown[]
  → arma newCards, pide nombre de deck (prompt, default = nombre del archivo),
    reemplaza Deck.cards/Deck.name, render() + onDeckLoaded()
  → alert final con total reconocido y detalle de líneas no reconocidas
```
Requiere `Modules/pdf.min.js` + `pdf.worker.min.js`; usa además `cdn.jsdelivr.net` para cmaps y fuentes estándar del PDF — única dependencia de red fuera de YGOProDeck (y Yugipedia) en toda la app. No crea claves nuevas de localStorage.

### 8.11 Alta rápida de cartas a Banlist/Genesys
```
Banlist.renderFormatList(formatName) → botón "＋ Agregar carta"
  → CardViewer.openCardSearch(formatName, prefillName?, mode?)
      → detecta modo puntos si Banlist.getData().formats[formatName].isGenesys
      → mini-buscador propio (fetch cardinfo.php?fname=) con overlay #cv-search-overlay
      → modo normal: chips Ban / Limitada / Semi-Limit → clic en resultado →
        Banlist.setCardStatus(formatName, cardId, {name,img}, status)
      → modo Genesys: stepper de puntos → clic en resultado →
        Banlist.setCardPoints(formatName, cardId, {name,img}, puntos)
      → repinta banlist-cards-${formatName} y el contador de cartas del formato
  → cada carta ya agregada a un formato Genesys puede reabrir sus puntos con
    CardViewer.openPointsEditor(formatName, cardId, cardName, currentPoints)
```
No agrega claves nuevas de localStorage — escribe sobre `Banlist.formats[...].cards`.

**Confirmado en esta pasada, no documentado antes:** `Banlist` soporta **varios formatos activos simultáneamente** (`getActiveFormats`/`toggleActiveFormat` — la v6.0 hablaba de "la Banlist activa" en singular), permite **crear/borrar formatos custom propios** (`createCustomFormat`/`deleteCustomFormat`, más allá de los predefinidos + Genesys), y cada formato tiene un toggle **"invertido"** (`toggleInverted`, probable modo whitelist en vez de blacklist — sin confirmar el efecto exacto).

### 8.12 Exportar/Importar Historial de Enfrentamientos (.txt)
```
Matchups.exportTXT()
  → junta this.getAll() (enfrentamientos) + Deck.getOptimizacion().sessions
    (sesiones completas de Optimización del deck activo)
  → descarga un .txt: cabecera de comentarios (#) + JSON {matchups, sessions}

Matchups.importTXT()
  → input[file] .txt → _mergeImported(text)
      → ignora líneas que empiezan con # antes de JSON.parse
      → por cada enfrentamiento: si el rival ya existe (case-insensitive), SUMA
        wins1st/losses1st/wins2nd/losses2nd y concatena notas; si no, lo crea.
        Compatible con exports viejos (array plano sin sessions)
      → _importRealSessions(sessions) — copia sesiones TAL CUAL al
        optimization_${deckName} del deck activo, con IDs regenerados; si todas
        las rondas de una sesión son del mismo rival, queda etiquetada
      → limpia el filtro activo de Matchups y repinta #mideck-optimizacion-pane
```
No afecta el cálculo de Duelista (sigue leyendo TODAS las rondas del deck activo). Cada registro de Matchups puede llevar `cardData` (decklist del rival vía `.ydk`), visible con `Matchups.openDeckPanel(index)`.

### 8.13 Reportar Error / Generar Reporte (Config) — *nuevo, confirmado a fondo en esta pasada*
Dos features distintas, sin relación entre sí pese a estar una al lado de la otra en la UI:

```
Config.generarReporte()   ["📋 Generar Reporte"]
  → si no existe window.DDLogger → alert de error, corta
  → confirm() con total de entradas y errores del log de la sesión
  → DDLogger.exportReport()   (idéntico a llamarlo desde la consola)

Config.abrirReportarError()   ["📧 Reportar Error"]
  → overlay con contador (dd_report_counter + 1, sin persistir todavía) y textarea
    para que el usuario describa el problema
  → Config.enviarReporte(counter, dateStr):
      1. arma loggerTxt (mismo contenido que DDLogger.exportReport(), reconstruido
         a mano — no reutiliza la función)
      2. arma configTxt: JSON con yugioh_config, yugioh_player_level,
         dd_player_profile, dd_content_visibility, yugioh_favoritas, yugioh_engines,
         yugioh_music_config, yugioh_meta_decks, yugioh_meta_fallbacks,
         yugioh_formacion_notes, yugioh_formacion_mastered,
         yugioh_formacion_fallbacks + todas las claves matchup_*
      3. descarga DD_Log_${stamp}.txt y DD_Config_${stamp}.txt (dos archivos)
      4. abre mailto:franq0524@gmail.com con asunto/cuerpo prellenado pidiendo
         adjuntar los 2 .txt manualmente
      5. localStorage.setItem('dd_report_counter', counter)  ← recién acá se persiste
      6. cierra el overlay
```
**Nota:** `configTxt` NO incluye combos, decks guardados (`deck_*`), Optimización, Complejidad ni Banlist — el "reporte de error" es más liviano que Exportar Data completa de Config. Si algún día se pide ampliar qué se manda en el reporte, es acá donde se toca la lista `claves` dentro de `enviarReporte`.

**No auditados a nivel de flujo interno en esta pasada** (se confirmó que existen y su inventario de métodos, pero no el paso a paso línea por línea): Línea de Combos completa (modo edición y modo ejecución), Gauntlet, Counters (CounterSim), Tu Experiencia con el Deck, Historial de Versiones, Zona de Práctica (resolución de cadena, información oculta, navegador de estados).

---

## 9. DEPENDENCIAS ENTRE MÓDULOS

> Base confirmada en la última auditoría línea-por-línea (módulos previos a Lobby/
> Combos/Gauntlet/CounterSim). La auditoría estructural v7.0 confirma que estos
> módulos existen con los nombres esperados y qué métodos tienen, pero el grafo de
> llamadas real (quién llama a quién dentro del cuerpo de cada función) sigue sin
> trazarse por completo — el addendum de abajo sigue siendo "probable", no
> confirmado línea por línea, salvo las líneas marcadas explícitamente.

```
data.js (ConfigManager)  <── buscador, mideck, historial, estadisticas,
                               simuladores, formacion, core (ContentManager)

data.js (Stats)          <── estadisticas, mideck (Deck.render usa
                               calculateInternalScore — ver 5.2 sobre G1/G2)

core.js (ContentManager) <── formacion (applyProfile al cambiar perfil)
                               navigation applyAll() en cada tab change

mideck.js (Engines)      <── Favoritas.render() delega si _activeTab='favoritas'
                               Staples: usa campo imageUrl (NO img)

mideck.js (Deck)         <── Optimización usa Deck.name como clave
                               (`optimization_${this.name}`), independiente de cards

historial.js (Duelista)  <── Lee EXCLUSIVAMENTE optimization_${Deck.name} (rondas del
                               deck activo) vía Deck.getOptimizacion() — ver 7.2. NO lee
                               matchup_${deckName}.

historial.js (Matchups)  <── selectDeckFilter()/clearDeckFilter() re-renderizan
                               #mideck-optimizacion-pane (Deck.renderOptimizacionPane)

formacion.js (Meta)      <── Sub-tabs Fuentes y Maestros de Formacion delegan a
                               Meta._renderFuentesSection() / Meta._renderMaestrosSection()

formacion.js (Welcome)   <── llama ConfigManager.savePlayerLevel() Y
                               ContentManager.applyProfile() al hacer enter()

formacion.js (TestDuelo) <── confirmado en esta pasada: NO depende de ningún otro
                               módulo, solo lee/escribe yugioh_custom_tests. Consumido
                               por Config → Test de Duelo y por Formación → Test
                               (autoría teórica/práctica).

decklists.js              <── confirmado en esta pasada: no depende de nada, es solo
(DecklistsData)                data estática. Consumido por Formación → Primeros Decks,
                               que pasa el .ydk crudo a Deck.parseYDK(content, filename).

default_data.js          <── requiere ConfigManager, Deck, Engines, Formacion,
(DefaultData)                 Meta, Banlist (todos ya cargados antes en index.html)
                               Parchea Config.resetToDefault al final del load.
```

**Addendum — módulos agregados después (inventario de métodos confirmado en esta
pasada v7.0; la relación exacta de llamadas entre ellos sigue sin trazarse línea por
línea salvo donde se indica):**
```
lobby.js (Lobby)         <── confirmado: depende de ConfigManager solo indirectamente
                               (no llama a sus métodos); usa dd_update_seen directo por
                               localStorage. Fetch propio a YGOProDeck para "Lo Nuevo"
                               (_fetchByRegion/_fetchLatest, sin pasar por otro módulo).
                               goSuggestion() SÍ llama a Navigation.showTab(s.tab)
                               directamente (confirmado). openCard() llama a
                               CardViewer.open(card._raw) (confirmado).

mideck.js (Combos)       <── confirmado: opera sobre Deck.cards para construir el pool
                               (_buildDeckPool) y llama a CardViewer.open (viewCard) /
                               Estadisticas (viewMetaCard) para ver cartas. Relación
                               exacta con autoAssignRoles() para la auto-detección de
                               starters/boss sigue sin confirmar a nivel de código.

simuladores.js           <── confirmado: CounterSim arma su pool desde Deck.cards /
(CounterSim, Gauntlet)        Engines.getAll() / Staples / Favoritas.getAll() (según
                               fuente elegida en el picker) y su resultado se puede
                               convertir en Engine (convertToEngine → Engines).
                               Gauntlet confirmado: lee yugioh_meta_decks y
                               yugioh_meta_card_library directo por localStorage
                               (_loadMetaSource) para las chips de decks del meta, y
                               depende de un pool manual o de ZonaPractica para jugar
                               las Pruebas (según selectPool/kind).

core.js (TabIntro)       <── confirmado que existe y su lógica interna (maybeShow,
                               getDisabledMap, sessionStorage para "ya mostrado"), pero
                               el punto exacto donde se dispara dentro de switchTab
                               sigue sin encontrarse (ver flujo 8.1) — no está en el
                               cuerpo de Navigation.showTab tal como se pudo inspeccionar
                               en esta pasada.
```
Antes de modificar cualquiera de estos módulos a fondo, confirmar la dependencia real leyendo el cuerpo completo de la función — el inventario de arriba dice QUÉ existe y a veces QUIÉN llama a quién, pero no reemplaza una lectura completa del flujo para cambios grandes.

---

## 10. API EXTERNA

**YGOProDeck** (sin auth, CORS libre) — base `https://db.ygoprodeck.com/api/v7/`:
```
cardinfo.php?id=                        búsqueda/lookup por ID (batch por comas)
cardinfo.php?fname=                     búsqueda difusa por nombre (Buscador; no se usa ?name=)
cardinfo.php?id=&misc=yes               usado en CardViewer.open() (ver 8.9) para sets,
                                         fechas de lanzamiento y disponibilidad MD/DL.
                                         tcg_date/ocg_date/formats viven DENTRO de
                                         misc_info[0], no en la raíz.
cardinfo.php?misc=yes&startdate=&dateregion=   usado por Lobby para "🆕 Lo Nuevo"
                                         (confirmado: dos llamadas en paralelo, una por
                                         dateregion=tcg y otra dateregion=ocg, mergeadas
                                         por ID — ver 4.1 y Lobby._fetchLatest)
archetypes.php                          lista de arquetipos
cardsets.php                            lista de packs/sets
cardinfo.php (sin filtros)              fetch completo, usado por DefaultData._initGenesys()
                                         y Deck._pdfFetchFullCardDB() (ver 8.10)

Imágenes:
https://images.ygoprodeck.com/images/cards/{id}.jpg        → full
https://images.ygoprodeck.com/images/cards_small/{id}.jpg  → small
```

**Yugipedia** (`yugipedia.com/api.php?action=parse&prop=wikitext`) — usado por `CardViewer._loadLore()` para el panel de Lore (ver 8.9, confirmado a fondo en esta pasada); fallback a proxy CORS (`api.allorigins.win`) si el fetch directo falla.

**pdf.js** (`Modules/pdf.min.js` + `pdf.worker.min.js`) — usado por `Deck.importPDF()` (ver 8.10); depende además de `cdn.jsdelivr.net` para cmaps y fuentes estándar del PDF en tiempo de ejecución.

**Mailto local (no es una API externa, pero es la única otra "integración de red" de la app):** `Config.enviarReporte()` y `TestDuelo.exportTest(id, 'send')` abren `window.location.href = 'mailto:franq0524@gmail.com?...'` con asunto/cuerpo prellenado — no envían nada por sí solos, dependen del cliente de correo del usuario.

---

## 11. CLASES CSS CLAVE

| Clase | Archivo CSS | Propósito |
|-------|------------|-----------|
| `.tab-content` / `.active` | style_core | Secciones de pestañas |
| `.engines-sidebar` | style_mideck | Sidebar 4 tabs Mi Deck |
| `.cv-overlay` | style_core | Visor de carta |
| `.cv-archetype-link` | style_buscador | Link "Arquetipo:" sin subrayado, llama a `CardViewer.openArchetypeInBuscador()` |
| `.cv-release-block` | style_buscador | Contenedor de "Lanzamiento TCG/OCG" y "Disponible en:" |
| `.cv-card-id` | style_buscador | En `var(--gold-color)` |
| `#welcome-overlay.welcome-visible` | style_core | Panel bienvenida |
| `.role-panel` / `.role-panel-header` | style_config | Panel colapsable de rol y nomenclatura |
| `.scoring-layers-grid` | style_config | Grid de columnas L1/L2/L4/L5 |
| `.scoring-row` / `.scoring-grid` | style_config | Filas en Scoring G1/G2 |
| `.config-section-block` / `.config-block-title` | style_config | Bloques dentro de Scoring Avanzado |
| `.scoring-layer-col` / `.scoring-layer-entry` | style_config | Columnas y entradas de capas |
| `.asb-bar-track` / `.asb-bar-fill` | style_simuladores | Barras de score (Internal/External) en Análisis |
| `.analysis-block` / `.analysis-block-title` | style_simuladores | Bloques dentro de Análisis de Deck |
| `.analysis-g1g2-row` / `.analysis-g1g2-item` / `.analysis-g1g2-profile` | style_simuladores | Pintan g1Score/g2Score/g1g2Profile en `renderDeckAnalysis()` |
| `.opt-*` (form, session-bar, badges…) | style_mideck | Panel de Optimización (deck testing) |
| `.cxd-*` (pregunta, opcion, bar-track/fill, result-card…) | style_mideck | Quiz y tarjeta de resultado de Complejidad del Deck |
| `.duelista-*` (card, level-block, progress-track, wr-grid…) | style_simuladores | Bloque "Nivel como Piloto del Deck", en Estadísticas y Mi Deck → Optimización |
| `.matchup-row-selected` / `.opt-filter-banner` | style_mideck | Estado visual del filtro Matchups → Historial de Sesiones |
| `#meta-update-overlay` | inline en estadisticas.js | Overlay de carga del Actualizar Scores |
| `.cv-contribution-block` / `.cv-contrib-row` / `.cv-contrib-bar-track` / `.cv-contrib-bar` / `.cv-contrib-val` / `.cv-contrib-delta` / `.cv-contrib-total` / `.cv-contrib-empty` / `.cv-contrib-section-title` / `.cv-deck-name` | style_buscador (probable, no confirmado) | Usadas desde `renderCardContribution()` — "Aporte al deck activo" |
| `.cv-role-chip` / `.cv-role-none` / `.cv-roles-row` | style_buscador (probable, no confirmado) | Chips de "🎯 Posibles Roles" en CardViewer |
| `.tdp-overlay` / `.tdp-panel` / `.tdp-close` / `.tdp-title` / `.tdp-counts` / `.tdp-loc-label` / `.tdp-cards-grid` / `.tdp-card` / `.tdp-qty` | **`style_simuladores.css` — resuelto y confirmado en v9.0** (14 ocurrencias; NO vive en `style_mideck.css` como se sospechaba en v8.0) | Panel "Ver Deck" del rival en Matchups (`Matchups.openDeckPanel`) |

**Confirmado en v9.0 (lectura real de los 6 CSS, no solo grep de nombres probables):** `.help-panel-*`/`.faq-*` (panel de Ayuda/FAQ, ver 4.8), `.lobby-*`, `.tabintro-*` y las clases del App Tour (`#apptour-*`, inyectadas dinámicamente por JS vía `<style>` propio, no en ningún archivo CSS del proyecto) viven mayormente en `style_core.css`; un subconjunto de `.tabintro-*`/`.lobby-*` también aparece en `style_config.css` (14 ocurrencias, ligado a la sección "Intro de Pestañas" de Config) y en `responsive_style.css` (6 ocurrencias, breakpoints propios).

**Pendiente de auditar por completo (los 6 archivos CSS no fueron parte de la pasada v7.0 — solo se auditó JS).** Ahora que se confirmó a nivel de JS que existen Lobby, Combos (con modo de ejecución), Gauntlet, CounterSim, Tu Experiencia con el Deck, Historial de Versiones, tema de fondo, TabIntro, y las 6 secciones nuevas de Config (Intro de Pestañas, Maestros del Duelo, Fuentes Externas del Meta, Juegos Alternativos, Temas de Formación, Test de Duelo), es prácticamente seguro que existen clases CSS propias para cada uno (`.lobby-*`, `.combo-*`, `.gauntlet-*`, `.cs-*`, `.exp-*`, `.tabintro-*`, `.error-report-*`, etc. — algunos nombres ya se vieron literalmente en el HTML generado por JS, ej. `.lobby-card`, `.lobby-suggestion-card`, `.tabintro-overlay`, `.tabintro-panel`, `.error-report-overlay`, `.error-report-panel`, `.exp-subtab-btn`). No se listan como confirmados en esta tabla porque no se leyó el CSS real — evitar asumir nombres exactos de propiedades/estilos al escribir CSS nuevo, revisar el archivo real primero.

---

## 12. NOTAS PARA TRABAJAR EFICIENTEMENTE

**Para cambios puntuales:** sube solo la función afectada indicando objeto y método.

**Para CSS:** todos los ajustes responsive van exclusivamente en `responsive_style.css`.

**Archivos que NO necesitas subir salvo cambios:**
- `html2canvas_min.js` — librería externa
- `pdf_min.js` / `pdf_worker_min.js` — librería externa (pdf.js, usada por `Deck.importPDF()`)
- `logger.js` — solo si cambia `TARGETS` o formato del reporte (recordar: `TARGETS` está al día salvo `TourGate`/`AppTour`, que faltan agregar — ver 1.2)

**Variables CSS globales** (en `style_core.css`):
`--gold-color` · `--accent-color` · `--bg-card` · `--text-light` · `--danger-color` · `--spacing-sm/md/lg`

**Diferencia clave entre los dos External Scores:**
- Deck activo → `Stats.calculateExternalScore()` → deck vs powerScoreCache
- Decks del meta → `calculateCrossExternalScores()` → N×N real

**Diferencia clave entre Peso de Rol y Poder Base:**
- `roleWeights` → escala 0.1–2.0 → Internal Score clásico (rendimientos decrecientes)
- `roleBasePower` → escala 0–10 → alimenta el G1/G2 Score

**Nota de diseño abierta (no es bug):** las vulnerabilidades G1/G2 del External Score reutilizan la tabla `g1g2Roles` (pensada para roles de carta) aplicada sobre nombres de specialty/mecánica. Confirmar con el usuario si se quiere una tabla G1/G2 propia para specialties antes de tocar `calculateExternalScore()`.

**Regla de oro para añadir una sección a Config:**
1. Añadir el bloque HTML en `Config.render()` con el ID de sección.
2. Definir `renderXxxSection()` que devuelve el HTML interno.
3. Definir los métodos save que llaman `ConfigManager.getConfig()` / `saveConfig(cfg)`.
4. Si hay datos nuevos en `defaultConfig`, añadir los getters correspondientes en `ConfigManager`.
Si se omite el paso 2, `Config.render()` lanza un TypeError silencioso y la pestaña queda en blanco. (Confirmado que este patrón se siguió al pie de la letra en las 6 secciones nuevas: Intro de Pestañas, Maestros del Duelo, Fuentes Externas del Meta, Juegos Alternativos, Temas de Formación y Test de Duelo — todas tienen su `renderXxxSection()` correspondiente.)

**Bug de la v6.0 — RESUELTO, confirmado en esta pasada:** `Deck.switchExperienciaTab` ya no está definido dos veces en `mideck.js`. Solo hay una definición y sí incluye `'cartas'` y `'sets'` en la lista de panes. No requiere acción.

**Discrepancia de la v6.0 sobre `decklists.js` — RESUELTA:** el `<script>` sí está cargado en `index.html` (entre `default_data.js` y `logger.js`), y `DecklistsData` existe tal como se documentaba en la prosa funcional (10 decks agrupados en Básico/Intermedio/Competitivo).

**Observación directa del `index.html` actual (no es bug confirmado, solo una asimetría a tener presente):** el botón de nav de Config es el único sin `onclick="switchTab(...)"` explícito — depende de que `Navigation` delegue por `data-tab`. Si algún día se toca `core.js` (Navigation) y Config deja de responder al clic, empezar a revisar por ahí antes que por `formacion.js` (Config vive ahí).

**Nuevo en esta pasada — atribución corregida:** `dd_report_counter` no pertenece a `DDLogger` como decía la v6.0; es la clave de `Config` para el contador de "📧 Reportar Error" (`Config.enviarReporte`), sin relación con `DDLogger.exportReport()`. Son dos sistemas de export de log completamente separados que coexisten en la misma UI (ver flujo 8.13).

**Nuevo en esta pasada — pendiente de decisión de producto:** `Banlist.toggleInverted(formatName)` existe y funciona, pero no está claro en esta auditoría estructural qué hace exactamente "invertido" a nivel de reglas (¿whitelist en vez de blacklist? ¿invierte el badge visual solamente?). Confirmar con una lectura del cuerpo de `getEffectiveBanStatus`/`getBadgeHTML` antes de tocar Banlist.